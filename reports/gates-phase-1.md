# Phase 1 gate checklist

Generated for PLAN.md P1-38 ("prove every Phase 1 gate in every environment SPEC requires, before releasing"). Each row below is one gate from SPEC.md Part A, listed individually from 1.01 through 1.52 (52 gates, no ranges), with its plain-language description (from SPEC.md), the actual test file(s) found in the codebase that verify it, and the actual test name. Result is filled in from the `gh workflow run ci.yml -f full=true` run recorded in PROGRESS.md's P1-38 row — that single run is what makes every "Pass" below true at once.

| Gate | Description | Test file | Test name | Result |
|---|---|---|---|---|
| 1.01 | A computer that hasn't used Comp HQ before shows "Who's using this computer?" with every current name as a button, in alphabetical order. | `e2e/people/picker.spec.ts` | "names appear as buttons in alphabetical order" | Pass |
| 1.02 | After picking a name, the top bar shows "You: *name*", and reopening the app on that computer goes straight in with the same name. | `e2e/people/picker.spec.ts`; `internal/app/integration_test.go` | "a picked name persists across a new page in the same context"; `TestPersonCookieAttributes` | Pass |
| 1.03 | Clicking "Change" shows the name list again, and picking a different name updates the top bar. | `e2e/people/picker.spec.ts` | "visiting the picker again allows switching to a different name" | Pass |
| 1.04 | "My name isn't here" lets you type a name; it's added, selected, and shows in the picker on other computers. | `e2e/people/picker.spec.ts` | "a name added in one context appears in another context's picker" | Pass |
| 1.05 | Renaming a person in Settings changes their name everywhere, including old history; removing a person takes them off the picker but old history still shows their name. | `e2e/settings/settings.spec.ts`; `e2e/kb/history.spec.ts` | "renaming a person shows the new name in the picker and top bar"; "removing a person leaves the picker"; "History shows a renamed person's new name, and a removed person's old name" | Pass |
| 1.06 | If the name chosen on a computer is removed, that computer shows the picker again the next time a page opens. | `e2e/people/picker.spec.ts` | "deactivating the current person shows the picker next time" | Pass |
| 1.07 | Every page shows the same frame — wordmark, the four sections, Settings, search box and current name — with the current section visibly marked. | `e2e/frame/gates.spec.ts` | "every registered route shows the frame, with the current section marked" | Pass |
| 1.08 | Briefing, Tasks and Calendar each show a friendly "Coming soon" page in this phase, never an error. | `e2e/frame/gates.spec.ts` | "placeholder sections show Coming soon with status 200" | Pass |
| 1.09 | No page needs sideways scrolling at any window size from 1024x700 to 1920x1080. | `e2e/frame/no-side-scroll.spec.ts` | `no sideways scroll: ${page name}` (one per registered page type, from `e2e/helpers/page-registry.ts`) | Pass |
| 1.10 | Every page passes an automatic accessibility check (colour contrast, labelled buttons and fields, usable by keyboard). | `e2e/a11y/pages.spec.ts`; `e2e/kb/editor.spec.ts`; `e2e/people/picker.spec.ts` | `axe: ${page name}` (one per page type); "publishing an article works with keyboard only"; "the picker works with keyboard only" | Pass |
| 1.11 | A web address that doesn't exist shows a friendly "Page not found" inside the normal frame, with a link to the Knowledge Base. | `e2e/frame/gates.spec.ts` | "an unknown address shows Page not found inside the frame" | Pass |
| 1.12 | Staff can create, rename and reorder categories; deleting one with articles in it is refused with a plain explanation, and deleting an empty one works. | `e2e/kb/categories.spec.ts`; `internal/kb/categories_test.go` | "categories: create, rename, reorder, and delete"; "deleting a category with articles is refused with a plain explanation"; `TestCategoryDeleteRefusedWithArticles` | Pass |
| 1.13 | The Knowledge Base home shows categories in the chosen order with article counts, plus the 10 most recently updated articles with who updated them and when. | `e2e/kb/categories.spec.ts`; `e2e/kb/articles.spec.ts` | "home page shows category tiles with article counts"; "the Knowledge Base home recent list shows who updated it and when" | Pass |
| 1.14 | Choosing a category, typing a title and content, and pressing Publish makes the article appear in its category straight away. | `e2e/kb/articles.spec.ts`; `internal/kb/articles_integration_test.go` | "publishing an article makes it appear in its category straight away"; `TestPublishArticleHTTPAppearsInCategory` | Pass |
| 1.15 | The editor toolbar offers two heading sizes, bold, italic, bulleted and numbered lists, link, table (with add/remove rows and columns) and image, each showing correctly once published. | `e2e/kb/editor.spec.ts` | `editor toolbar (SPEC gate 1.15)` describe block: "large heading", "small heading", "bold and italic", "bullet list", "numbered list", "link", "table with add/remove row and column" | Pass |
| 1.16 | Pasting a screenshot shows it in the editor within 3 seconds; after publishing, the image is served by Comp HQ itself, not any other website. | `e2e/kb/images.spec.ts` | "@fresh pasting a screenshot shows it within 3s, publishes locally-served, and lands on disk" | Pass |
| 1.17 | Dragging in a JPEG, PNG, GIF or WebP up to 20 MB inserts it; a larger or unsupported file shows a plain message and changes nothing. | `e2e/kb/images.spec.ts` | "dropping a ${mime} image inserts it" (one per image type); "a file larger than 20 MB shows a plain message and changes nothing"; "a file that is neither an image nor a Word document shows a plain message and changes nothing" | Pass |
| 1.18 | Pasting a web page or Word document keeps structure (headings, lists, links, tables, images) but drops fonts, colours and sizes; pictures that can't come across from Word are marked with the exact message. | `e2e/kb/paste-cleanup.spec.ts`; `internal/kb/html/sanitize_test.go` | "pasting a web page keeps structure and images, publishing strips all styling"; "pasting from Word marks unreachable pictures with the exact message"; `TestSanitizeStripsWebPageFixtureStyles` | Pass |
| 1.19 | Publishing copies images hosted on other websites onto the NAS; if a copy can't be made, the editor names and marks the failed images and still saves the text. | `e2e/kb/external-images.spec.ts`; `internal/kb/external_images_integration_test.go` | "@fresh an external image reachable at publish time is copied locally"; "an unreachable external image is named, marked, and the rest of the text is saved"; `TestPublishArticleHTTPCopiesExternalImage`; `TestPublishArticleHTTPReportsUnreachableImage` | Pass |
| 1.20 | Leaving the editor with unsaved changes asks "Leave without saving?" first. | `e2e/kb/editor.spec.ts` | `leaving the editor (SPEC gate 1.20)` describe block: "Cancel on a dirty editor asks first, and dismissing it stays put"; "navigating away from a dirty editor triggers the browser prompt" | Pass |
| 1.21 | If two people edit the same article and both publish, the second sees a conflict message, keeps their own text on screen, and can publish anyway; the other version stays in history. | `e2e/kb/edit-conflict.spec.ts`; `internal/kb/articles_test.go` | "a second publisher sees the conflict message, keeps their text, and can publish anyway"; `TestPublishDetectsVersionConflict`; `TestPublishOverrideBypassesConflictAndKeepsBothInHistory` | Pass |
| 1.22 | Every publish is recorded; the article's History lists each version with name, date and time, newest first. | `e2e/kb/history.spec.ts`; `internal/kb/history_integration_test.go` | "History lists each version newest first with name and time"; `TestHistoryHTTPListsVersionsNewestFirst` | Pass |
| 1.23 | Opening an old version shows it as it was; "Restore this version" makes it current and adds a new history entry with the restorer's name. | `e2e/kb/history.spec.ts`; `internal/kb/history_integration_test.go` | "an old version displays as it was and can be restored"; `TestVersionAndRestoreHTTP` | Pass |
| 1.24 | Archiving an article removes it from its category and search and lists it in "Archived articles"; restoring puts it back in both. | `e2e/kb/history.spec.ts`; `internal/kb/history_integration_test.go` | "archiving hides an article from its category and lists it in Archived; unarchiving brings it back"; `TestArchiveAndUnarchiveHTTP` | Pass |
| 1.25 | Nowhere in the app can an article be permanently deleted. | `internal/kb/history_integration_test.go` | `TestNoDeleteRouteForArticles` | Pass |
| 1.26 | The search box is on every page; after 2+ letters, up to 20 matching articles appear within 1 second, most relevant first, with title matches ranked above body matches. | `e2e/kb/search.spec.ts` | "gate 1.26: title matches rank above body matches, and short input shows nothing" | Pass |
| 1.27 | Each search result shows the article title, its category, and the matching passage with the search words highlighted. | `e2e/kb/search.spec.ts` | "gate 1.27: a result shows the title, category and a highlighted passage" | Pass |
| 1.28 | Searching "toner" shows the printer article highlighted, and clicking it scrolls to and highlights that paragraph. | `e2e/kb/search.spec.ts` | "gate 1.28: search result scrolls to and highlights the matching passage" | Pass |
| 1.29 | "printers" finds an article containing "printer", and "ton" finds "toner" (stemming and prefix matching). | `e2e/kb/search.spec.ts`; `internal/kb/search/search_test.go` | "gate 1.29: stemming and prefix matching find related words"; `TestSearchStemmingAndPrefix` | Pass |
| 1.30 | A newly published article, or a new word added in an edit, is found by the very next search; words removed in the edit no longer find it. | `e2e/kb/search.spec.ts` | "gate 1.30: publishing and editing are reflected in the very next search" | Pass |
| 1.31 | Archived articles never appear in search results. | `e2e/kb/search.spec.ts`; `internal/kb/search_integration_test.go` | "gate 1.31: an archived article is excluded from search"; `TestSearchJSONHTTPExcludesArchivedArticle` | Pass |
| 1.32 | A search with no matches says "No articles match *words*"; typing symbols such as `" * ( - :` never causes an error. | `e2e/kb/search.spec.ts`; `internal/kb/search/search_test.go` | "gate 1.32: no-match message and symbol fuzzing cause no errors"; `TestSearchSymbolFuzzingNeverErrors` | Pass |
| 1.33 | With the test library loaded, every Knowledge Base page is ready within 1.5 seconds, and search results appear within 1 second. | `e2e/speed/kb.speed.spec.ts` | `Knowledge Base speed (SPEC gate 1.33)` describe block: "navigation to data-ready stays within budget for every page type"; "last keystroke to the search panel rendering stays within budget" | Pass |
| 1.34 | Settings -> Export everything downloads one file that, unpacked offline, opens a contents page listing every article by category (archived ones marked), each with its formatting and images. | `e2e/settings/export.spec.ts`; `internal/kb/export_test.go` | "exported zip opens offline with every article, its image, and archived ones marked"; `TestBuildExportRewritesImageSrcsRelativeToArticleAndCollectsThem` | Pass |
| 1.35 | Comp HQ backs up automatically every night and keeps the last 30; Settings shows the last backup time, and a warning banner shows on every page if it's more than 2 days old. | `e2e/settings/backups.spec.ts`; `internal/db/backup_test.go`; `internal/db/scheduler_test.go` | "a stale backup shows the warning banner on every registered page"; "no banner shows with a fresh last backup"; `TestDailyBackupOverwritesSameDayAndPrunesOldest`; `TestRunBackupSchedulerRunsImmediatelyWhenStale` | Pass |
| 1.36 | Settings -> Content check shows how many articles contain images not stored on the NAS, and lists them. | `e2e/settings/content-check.spec.ts`; `internal/kb/content_check_test.go` | "a clean article is not listed in Content check"; "an article with a failed external image is listed in Content check"; `TestListContentCheckListsPublishedAndArchivedWithIssuesOnly` | Pass |
| 1.37 | Settings -> About shows the version number. | `e2e/settings/settings.spec.ts` | "About shows the version" | Pass |
| 1.38 | Comp HQ installs by pasting one provided settings file into TrueNAS's "Install via YAML" screen and filling in the folder and time zone, as a single app with nothing else to install. | `deploy/test/container-test.sh` | step 1: "docker compose config validates" | Pass |
| 1.39 | After the app restarts (including a NAS reboot), Comp HQ is back on its own within 60 seconds, with all articles, images, history and names intact. | `deploy/test/container-test.sh`; `e2e/smoke/basic.spec.ts`; `e2e/smoke/names.spec.ts` | steps 2-3: `wait_healthy` (60s deadline) and data-folder checksum before/after restart and full down/up; "@seed the test library"; "@verify the seeded data is present"; "@names-seed adds a fixed-name person"; "@names-verify the person is still in the picker" | Pass |
| 1.40 | All data (articles, images, history, names, backups) lives in the one Comp HQ folder on the NAS; removing and reinstalling the app, pointed at the same folder, brings everything back. | `deploy/test/container-test.sh`; same smoke tests as 1.39 | step 4: "recreate: remove the container and the image, then bring it back up", checksum plus "@verify"/"@names-verify" after | Pass |
| 1.41 | With all internet access blocked, every Phase 1 gate still passes (Import from Word included), except copying images from other websites, which reports clearly that it couldn't. | `deploy/test/container-test.sh`; `e2e/smoke/offline.spec.ts`; full `e2e` Playwright project run against the offline container | step 5: "offline: the app can't reach the internet, and the full E2E suite passes"; "the app answers healthz but cannot reach the internet"; every non-`@fresh` E2E test, including "an unreachable external image is named, marked, and the rest of the text is saved" for the 1.19-under-1.41 carve-out | Pass |
| 1.42 | Installing a newer version keeps all data; switching back to the previous version still works with that data. | `deploy/test/container-test.sh` | `run_upgrade_rollback_test` function (comment: "proves gate 1.42"): runs "@seed"/"@verify"/"@verify-prev" against the previous release, the current build, and back | Pass |
| 1.43 | Settings -> Set up this computer shows the exact shortcut text for Chrome and Brave with this Comp HQ's address filled in and a Copy button; launching the browser with it opens Comp HQ with no tabs or address bar. | `internal/settings/setup_test.go`; `e2e/settings/app-window.spec.ts`; `e2e/settings/settings.spec.ts` | `TestBuildSetupCommands`; "the --app command opens Comp HQ as the first window and it becomes ready"; "Copy puts the exact command text on the clipboard" | Pass |
| 1.44 | A plain-language README exists covering opening Comp HQ, setting up a computer, updating, undoing an update, backing up, restoring, and "it won't open — what now?" | `tools/policy/policy_test.go` | `TestReadmeHasRequiredHeadings` | Pass |
| 1.45 | The editor's Import from Word button, or dragging a .docx onto the editor, opens the file; a .docx downloaded from Google Docs imports too. | `e2e/kb/import-word.spec.ts`; `e2e/kb/images.spec.ts` | "importing sample.docx by button shows headings, lists, table spans, pictures, placeholders and the title — without publishing"; "importing sample.docx by drop shows the same result as the button"; "googledocs.docx imports"; "dropping a .docx imports it instead of uploading it as an image" | Pass |
| 1.46 | Importing the sample Word document puts it in the editor without publishing, keeping headings, bold, italic, lists (including indented levels), links and tables, with fonts, colours and sizes removed. | `internal/kb/docx/docx_test.go`; `e2e/kb/import-word.spec.ts` | `TestConvertSampleDocxTitleAndHeadings`; `TestConvertNestedThreeLevelsMixedBulletsAndNumbers`; `TestConvert3x3TableWithMergesAndHeaderRow`; "importing sample.docx by button shows headings, lists, table spans, pictures, placeholders and the title — without publishing" | Pass |
| 1.47 | Pictures in the document appear in the editor in their places; after publishing, each is served by Comp HQ itself and keeps its Word alt text. | `internal/kb/docx/docx_test.go`; `e2e/kb/import-word.spec.ts` | `TestConvertDrawingPictureExtractsImageWithAlt`; `TestConvertVMLPictureExtractsImage`; "importing sample.docx by button shows headings, lists, table spans, pictures, placeholders and the title — without publishing" | Pass |
| 1.48 | If the document starts with a Title or Heading 1 paragraph, that text becomes the article title; otherwise the file name is used, and it can be changed before publishing. | `internal/kb/docx/docx_test.go` | `TestConvertTitleFromFirstHeading1`; `TestTitleFromFilenameFallback` | Pass |
| 1.49 | Items the converter can't bring across (charts, diagrams, shapes, equations, unusable embedded objects, unsupported picture formats) each become a clearly marked placeholder in the article. | `internal/kb/docx/docx_test.go`; `e2e/kb/import-word.spec.ts`; `e2e/settings/content-check.spec.ts` | `TestConvertDiagramGraphicDataBecomesPlaceholder`; `TestConvertUnsupportedImageFormatBecomesPlaceholder`; `TestConvertObjectWithoutImageBecomesPlaceholder`; "importing sample.docx by button shows headings, lists, table spans, pictures, placeholders and the title — without publishing"; "an imported article with a chart placeholder is listed until fixed and republished" | Pass |
| 1.50 | Importing when the editor already has content asks "Replace what's in the editor with this document?" first; Cancel changes nothing, and importing into an existing article and publishing adds a new, restorable history entry. | `e2e/kb/import-word.spec.ts` | "importing into an existing article confirms the replace, and the old version still restores after publishing" | Pass |
| 1.51 | A file Comp HQ can't read (old .doc, password-protected, PDF, or damaged) shows the exact plain-language message to re-save it as .docx; a file over 50 MB is refused with a plain message; nothing else changes. | `internal/kb/docx/docx_test.go`; `internal/kb/import_docx_integration_test.go`; `e2e/kb/import-word.spec.ts` | `TestConvertBadFixtures`; `TestImportDocxHTTPBadFixturesShowExactMessage`; `TestImportDocxHTTPOversizeFileRejected`; "each bad fixture and an oversize file show the exact message and leave the editor unchanged" | Pass |
| 1.52 | A 20-page document with 10 pictures is in the editor within 10 seconds of choosing it. | `e2e/kb/import-word.spec.ts` | "a 20-page document with 10 pictures is in the editor within 10 seconds" | Pass |

**Also asserted in CI, not tied to one gate:** `tools/policy/policy_test.go`'s `TestDeployYAMLRules` (no `COMPHQ_TEST_MODE` in `deploy/truenas.yaml`), `TestWorkflowActionsPinnedBySHA`, `TestDockerfileFromPinnedByDigest`, `TestNoRawURLsInTemplatesOrStatic`, `TestGoModAtMostFiveDirectRequirements`.

**Not yet applicable in Phase 1:** 2.xx and 3.xx gates (Phase 2/3 features), and 2.22's CSV part of the export (P2-18).

**CI run:** https://github.com/stas-comp/comphq/actions/runs/35250669689 (`gh workflow run ci.yml -f full=true`, all five jobs — go, browser, container, speed, screens — green). Two earlier dispatches of this same run (35247465972, 35248677291) failed and are not the run this checklist's results describe: they caught real, now-fixed issues (a checksum step unreadable-by-a-different-user permission bug, and three E2E tests that only worked against a per-worker local server) — see docs/decisions.md D-37 and the P1-38 commit history.
