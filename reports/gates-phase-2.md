# Phase 2 gate checklist

Generated for PLAN.md P2-19 ("prove every Phase 1 and Phase 2 gate in every environment SPEC requires, before releasing"). Each row below is one Phase 2 gate from SPEC.md Part A, listed individually from 2.01 through 2.38 (38 gates, no ranges), with its plain-language description, the actual test file(s) found in the codebase and the actual test name(s). Result is filled in from the `gh workflow run ci.yml -f full=true` run recorded in PROGRESS.md's P2-19 row — that single run is what makes every "Pass" below true at once. The 52 Phase 1 gates are re-proved by the same run; `reports/gates-phase-1.md` still maps them, with the changes below.

**Phase 1 gates whose tests changed in Phase 2 (PLAN.md rule 2.5):** 1.08 no longer lists Tasks (P2-01) or Calendar (P2-14) as "Coming soon" — only Briefing remains, until Phase 3; 1.07, 1.09 and 1.10 now also cover every Tasks and Calendar page type through `e2e/helpers/page-registry.ts`; 1.34 (Export everything) is unchanged and still passes alongside 2.22.

| Gate | Description | Test file | Test name | Result |
|---|---|---|---|---|
| 2.01 | The Tasks **Board** view shows four columns, in order: Ideas, To do, In progress, Done. | `e2e/tasks/board.spec.ts` | gate 2.01: Board shows Ideas, To do, In progress, Done in order | Pass |
| 2.02 | Adding a task with just a title puts it at the bottom of Ideas (or the column chosen) for everyone. Notes, people and due date are optional. The creator is recorded. | `e2e/tasks/board.spec.ts`; `internal/tasks/handlers_integration_test.go` | gate 2.02: a title-only task is visible in a second context after reload; gate 2.02: a task can be added directly into a chosen column; TestCreateTaskHTTPAppearsOnBoard | Pass |
| 2.03 | Cards show the title, assigned people, and due date. An unfinished task past its due date shows an OVERDUE stamp. | `e2e/tasks/board.spec.ts` | gate 2.03: a card shows its assigned people and due date; @fresh gate 2.03: an unfinished task past its due date shows OVERDUE | Pass |
| 2.04 | Dragging a card to another column moves it, and dragging within a column reorders it. Both are the same on every computer after refresh. Every card also has Move up, Move down and Move to… buttons that do the same without dragging. | `e2e/tasks/board.spec.ts`; `internal/tasks/order_test.go` | gate 2.04: Move up and Move down reorder cards, visible in a second context; gate 2.04: a task can be moved with keyboard only; @fresh dragging within a column reorders it, and the order survives a reload; @fresh dragging a card to another column moves it there | Pass |
| 2.05 | The To do header reads "Top = most important". A card moved into To do by button goes to the bottom, and a dragged card lands where it's dropped. | `e2e/tasks/board.spec.ts` | gate 2.05: Move to… lands a card at the bottom of the target column | Pass |
| 2.06 | Opening a card allows editing all its details. Its Activity list shows who created it, and who moved it, changed its people, or changed its due date, with dates. | `e2e/tasks/details.spec.ts`; `internal/tasks/details_test.go` | gate 2.06: opening a card allows editing every detail, and Activity records what changed | Pass |
| 2.07 | "My tasks" shows only cards assigned to the current person. Choosing a person shows only theirs. Typing a word shows only cards with that word in the title. | `e2e/tasks/filters.spec.ts` | gate 2.07: My tasks, a chosen person, and a word each narrow the board | Pass |
| 2.08 | A card in Done for more than 14 days no longer shows on the board. It appears in "Finished tasks", newest first, where "Reopen" returns it to the bottom of To do. | `e2e/tasks/finished-removed.spec.ts`; `internal/tasks/lifecycle_test.go` | gate 2.08: a task done over 14 days ago moves to Finished tasks, and Reopen returns it to To do | Pass |
| 2.09 | "Remove task" hides it from the board and calendar and lists it in "Removed tasks", where it can be restored. Nothing can be permanently deleted. | `e2e/tasks/finished-removed.spec.ts`; `internal/tasks/handlers_integration_test.go` | gate 2.09: Remove hides a task from the board and lists it in Removed tasks; Restore brings it back; TestNoDeleteRouteForTasks | Pass |
| 2.10 | A change made on one computer appears on another computer's board within 60 seconds, or straight away when that window is clicked back into. A card someone is part-way through editing is never wiped out by this. | `e2e/tasks/refresh.spec.ts` | gate 2.10: a change on one computer appears on another within 60 seconds; a change appears immediately after the window is focused; a change is held back while the page is busy, and the notice applies it | Pass |
| 2.11 | A removed person no longer appears when assigning, but still shows on their old tasks, marked "(removed)". | `e2e/tasks/removed-people.spec.ts` | gate 2.11: a removed person no longer appears when assigning, but still shows on their old tasks marked (removed) | Pass |
| 2.12 | The Calendar opens on this month: weeks start Monday, Saturdays are highlighted, and today is marked. It has Previous, Next and Today buttons. The List view shows the next 12 weeks in date order. | `e2e/calendar/calendar.spec.ts` | gate 2.12: month view (Saturdays highlighted, today marked, and navigation works; clicking a day starts a new event on that date); gate 2.12: list view (shows events in date order across 12 weeks, excluding anything further out) | Pass |
| 2.13 | An event can be added with a title (required), date, optional end date, all-day or start time with optional end time, notes, repeats (Never / Weekly / Monthly / Yearly) with optional "until" date, and "Show in briefing ahead" (not early, or a number of days or weeks; the default is 1 week). | `e2e/calendar/calendar.spec.ts`; `internal/calendar/handlers_integration_test.go` | gate 2.13: every field is saved and redisplayed; an event can be added with keyboard only; TestCreateEventHTTPRedisplaysEveryField and the three validation tests | Pass |
| 2.14 | Weekly events repeat on the same weekday. Monthly events repeat on the same date, and an event on the 31st shows on the last day of shorter months. Yearly events repeat on the same date, and 29 February shows on 28 February in other years. Nothing appears after the "until" date. | `internal/calendar/recur/recur_test.go` | TestWeeklyOnTheSameWeekday; TestMonthlyOnThe31stClamped; TestYearlyOn29FebClampsInNonLeapYears; TestUntilDateBoundaryIsInclusive; TestMultiDayAcrossMonthEnd; TestRangeAcrossYears | Pass |
| 2.15 | A yearly Christmas concert on Sat 12 Dec 2026, moved with "Change just this one" to Sat 19 Dec 2026, shows on 19 Dec 2026 and still shows on 12 Dec 2027. | `e2e/calendar/calendar.spec.ts`; `internal/calendar/recur/recur_test.go` | gate 2.15: moving just one occurrence of a repeating event leaves other years unaffected; TestMovedExactGate215Dates | Pass |
| 2.16 | "Cancel just this one" hides only that occurrence, and the others remain. | `e2e/calendar/calendar.spec.ts`; `internal/calendar/recur/recur_test.go` | gate 2.16: cancelling just one occurrence leaves the others; TestCancelledOccurrenceIsRemoved | Pass |
| 2.17 | "Change all" (for example a new title or notes) shows on every occurrence. An occurrence moved on its own keeps its own date. | `e2e/calendar/calendar.spec.ts`; `internal/calendar/recur/recur_test.go` | gate 2.17: Change all updates every occurrence, and a moved one keeps its date with the new title; TestMovedOccurrenceStillUsesSeriesTitleAndNotes | Pass |
| 2.18 | "Remove event" hides the whole event and lists it in "Removed events", where it can be restored. | `e2e/calendar/calendar.spec.ts`; `internal/calendar/handlers_integration_test.go` | gate 2.18: Remove hides an event and lists it in Removed events, and Restore brings it back; TestNoDeleteRouteForCalendar | Pass |
| 2.19 | Unfinished tasks with due dates show on their date, looking different from events. Clicking one opens the task. Finished and removed tasks don't show. | `e2e/calendar/calendar.spec.ts`; `internal/calendar/handlers_integration_test.go`; `internal/tasks/due_test.go` | gate 2.19: a due task shows as an outlined chip and opens the task, but finished and removed tasks don't show; TestMonthViewHTTPShowsTaskDeadlineChip; TestDueTasksExcludesDoneAndRemoved | Pass |
| 2.20 | An event's details show who last changed it and when. | `e2e/calendar/calendar.spec.ts` | gate 2.20: details show who last changed it and when, updated after an edit | Pass |
| 2.21 | With the test library loaded, the Tasks board and a Calendar month are each ready within 1.5 seconds. | `e2e/speed/tasks.speed.spec.ts`; `e2e/speed/calendar.speed.spec.ts` | navigation to data-ready stays within budget for every Tasks page; month and list views stay within budget | Pass |
| 2.22 | Export everything now also includes a tasks spreadsheet and an events spreadsheet. Both open in Excel with clear column names and readable dates. | `e2e/settings/export.spec.ts`; `internal/settings/export_integration_test.go` | exported zip includes tasks.csv and events.csv with clear headers and readable dates; TestExportHTTPZipContainsIndexArticlesAndDatabase | Pass |
| 2.23 | Every Tasks and Calendar page passes the accessibility and window-size checks (as 1.09 and 1.10). | `e2e/a11y/pages.spec.ts`; `e2e/frame/no-side-scroll.spec.ts`; `e2e/speed/calendar.speed.spec.ts`; `e2e/speed/tasks.speed.spec.ts`; `each page's own spec` | axe: / no sideways scroll: (one per registered Tasks and Calendar page type); every Calendar page passes standard page checks with the seeded library; Board, a task page, Finished and Removed pass standard page checks with the seeded library | Pass |
| 2.24 | The Tasks page has a **My jobs / Board / Team** switch. Team shows an **Unassigned** column first, then one column per current person in alphabetical order. | `e2e/tasks/team.spec.ts` | Team view: lane order, grouping, numbering, and workload blocks | Pass |
| 2.25 | Each person's column lists their unfinished jobs in two groups: **Working on now** (In progress) and **Up next** (To do, numbered in priority order). Ideas and finished jobs don't appear. The Unassigned column lists To do and In progress jobs that nobody is on. | `e2e/tasks/team.spec.ts`; `internal/tasks/lanes_test.go` | Team view: lane order, grouping, numbering, and workload blocks; the lanes unit tests | Pass |
| 2.26 | Every task has a size: Small, Medium or Large. New tasks are Medium unless changed, and the size can be changed when editing. Cards on the board and in the Team view show the size, and a size change appears in the task's Activity. | `e2e/tasks/details.spec.ts` | gate 2.26: cards on the board show their size | Pass |
| 2.27 | Each person's column shows their workload as a row of blocks (small = 1, medium = 2, large = 4) and a line such as "1 large · 1 medium · 1 small". Someone with one large job shows the same number of blocks as someone with four small jobs. No warning colours or limits appear. | `e2e/tasks/team.spec.ts`; `internal/tasks/lanes_test.go` | Team view: lane order, grouping, numbering, and workload blocks; the workload unit tests | Pass |
| 2.28 | Dragging a job from Unassigned onto a person assigns it to them. Dragging a job from one person to another takes the first person off it and puts the second on, and anyone else on the job stays. The job's column and priority don't change. An "Assign to…" button does the same without dragging. | `e2e/tasks/team-assign.spec.ts`; `internal/tasks/assign_test.go` | @fresh gate 2.28: drag assigns from Unassigned to a person, and from one person to another while keeping a third; gate 2.28: the Assign to… button assigns the same way as dragging | Pass |
| 2.29 | Reordering a person's Up next jobs (by dragging or with Move up/down) changes the shared priority order. On the board, the moved job sits directly above or below the job it was placed next to, and all other jobs keep their order. | `e2e/tasks/team-assign.spec.ts` | gate 2.29: reordering a lane's Up next changes only the moved task's position in the shared order | Pass |
| 2.30 | A job with two people shows in both of their columns and counts towards both workloads. | `e2e/tasks/team.spec.ts`; `internal/tasks/lanes_test.go` | Team view: lane order, grouping, numbering, and workload blocks (a shared job in both lanes) | Pass |
| 2.31 | With the test library loaded (2,000 tasks, 10 people), the Team view is ready within 1.5 seconds and passes the accessibility and window-size checks. When there are more people than fit, the columns scroll sideways inside their own area and the page itself doesn't. | `e2e/tasks/team-assign.spec.ts`; `e2e/speed/tasks.speed.spec.ts` | gate 2.31: with many people, the lanes scroll sideways and the page does not; Team view passes standard page checks with the seeded library | Pass |
| 2.32 | Opening Tasks always shows **My jobs** first, for the name picked on that computer. | `e2e/tasks/myjobs.spec.ts` | gate 2.32: a fresh name on a computer lands on My jobs | Pass |
| 2.33 | My jobs shows two areas. **My jobs** holds the person's Working on now and Up next jobs (numbered by priority) with their workload blocks, matching their Team view column. **Up for grabs** holds jobs nobody is on (In progress and To do, in priority order), then ideas nobody is on. Jobs belonging only to other people, and finished jobs, don't appear. | `e2e/tasks/myjobs.spec.ts` | gate 2.33: My jobs shows the person's own lane and Up for grabs, and nothing else | Pass |
| 2.34 | Dragging a To do job from Up for grabs into My jobs puts the person on it, placed where it was dropped in their Up next. Pressing **Take it** does the same but keeps the job's current place in the priority order. | `e2e/tasks/myjobs.spec.ts`; `internal/tasks/take_test.go` | @fresh gate 2.34: take by drag places the job where it was dropped; gate 2.34: take by button keeps the job's place in the shared priority order | Pass |
| 2.35 | Taking an idea (by dragging or Take it) puts the person on it and moves it to To do, where it was dropped, or at the bottom when Take it is used. The task's Activity records both changes. | `e2e/tasks/myjobs.spec.ts`; `internal/tasks/take_test.go` | gate 2.35: taking an idea moves it to To do | Pass |
| 2.36 | A taken job leaves Up for grabs on every computer within 60 seconds, or straight away when that window is clicked back into. If two people try to take the same job at almost the same moment, the second person sees "Sam has just taken this job", and the job isn't added to their list. | `e2e/tasks/myjobs-conflict.spec.ts` | gate 2.36: two people taking the same job, the first succeeds and the second sees the exact message; a taken job leaves Up for grabs within 60 seconds and immediately on focus | Pass |
| 2.37 | From My jobs a person can move a job to In progress or Done with buttons. A job marked Done leaves My jobs and appears in Finished tasks. **Give back** takes the person off a job, and if nobody else is on it, it returns to Up for grabs. | `e2e/tasks/myjobs-conflict.spec.ts` | gate 2.37: Done leaves My jobs, and reaches Finished tasks after 14 days; gate 2.37: Give back returns a job to Up for grabs, but not while someone else is still on it | Pass |
| 2.38 | With the test library loaded, My jobs is ready within 1.5 seconds and passes the accessibility and window-size checks. | `e2e/speed/tasks.speed.spec.ts` | navigation to data-ready stays within budget for every Tasks page; My jobs passes standard page checks with the seeded library | Pass |
