# Comp HQ — Build Progress

Build agent: follow `PLAN.md` §2. Work on the first row that isn't `done`. Keep this file accurate at the end of every session.
Status values: `todo` · `in-progress` · `blocked` · `done`.

## Tasks

| ID | Title | Status | Commit(s) | CI run | Notes |
|---|---|---|---|---|---|
| P1-01 | Local tooling | done | | | go1.27.0, node v24.19.0, npm 11.17.0, git 2.37.1, gh 2.101.0; docker engine: not running (expected, container tests run in CI only). Installed via winget. Note: this machine's terminal tools (used by the build agent) don't pick up a freshly-installed program's PATH until the agent's own session restarts, so each command in this session that needs go/node/npm/gh explicitly adds their folders to PATH first; this doesn't affect the owner's own use of the computer. |
| P1-02 | GitHub sign-in and repository (STOP S1) | done | 90a9caf | | Repo stas-comp/comphq created (private), pushed to main. D-01 recorded. |
| P1-03 | Go skeleton: binary, config, health, migrations runner | done | 759f02f | no CI yet (added in P1-05) | go vet + go test and integration tests pass locally; `go run ./cmd/comphq` serves `/` with `<body data-ready>` and `/healthz` returns 200. D-02, D-03 recorded. |
| P1-04 | Playwright and axe skeleton | done | 0939f46 | no CI yet (added in P1-05) | `npm run check` passes locally from both PowerShell and Bash. Added placeholder `tools/policy` package so the command surface works before P1-05 (see D-04a). |
| P1-05 | CI workflow and repository policy tests | done | d8cd122 | https://github.com/stas-comp/comphq/actions/runs/35113839555 | First CI run (12f0ef7) and the follow-up PROGRESS.md update (d8cd122) both green: `go` and `browser` jobs pass; `container`/`speed`/`screens` correctly skipped (no flag/tag). `.github/workflows/ci.yml` added. Real `tools/policy` checks replace the P1-04 placeholder. D-03, D-04 recorded. |
| P1-06 | Dockerfile, TrueNAS YAML, container tests, tag v0.0.1 | done | 2e8094b | https://github.com/stas-comp/comphq/actions/runs/35115590961 | Container job green (2 fixes needed: sudo for uid-568-owned files; excluded SQLite -wal/-shm from the data checksum, see docs/decisions.md). Full dispatch green (run 35116031926). Tag v0.0.1 pushed; both CI (35116427243) and Release (35116427158, incl. publish) green. CHANGES.md has the 0.0.1 entry. D-02, D-04b, D-06a, D-06b, D-08 recorded. |
| P1-07 | Make the image public (STOP S2) | done | c3028bc | | Owner made the ghcr.io package public. Verified anonymously: token request + HEAD/GET on the 0.0.1 manifest both return 200 with no auth header. |
| P1-08 | Spike: FTS5 search with block ids | done | 7263076 | https://github.com/stas-comp/comphq/actions/runs/35126541585 | `internal/kb/search` (query builder, ranking, snippet/highlight) and `migrations/kb/0001_search.sql` added. All 6 unit tests green on Windows locally and Linux CI. D-10 recorded. |
| P1-09 | Spike: editor bundle | done | 42752cc | https://github.com/stas-comp/comphq/actions/runs/35127980709 | TipTap 3.31.3 bundled via esbuild (`web/static/vendor/editor/editor-3.31.3.js`, reproducible — verified locally and in CI via `git diff --exit-code`). Added global CSP/Referrer-Policy headers, `/static/` file serving, and the `COMPHQ_TEST_MODE`-gated `/__test/editor` harness. 12 E2E tests green (every toolbar command, paste callback, zero CSP violations). D-05 recorded; D-11 not needed. |
| P1-10 | Spike: clipboard paste in headless Chromium (send N1) | done | 1b276dd | https://github.com/stas-comp/comphq/actions/runs/35128748769 | `e2e/helpers/paste.ts` (synthetic ClipboardEvent/DragEvent method, D-12), paste fixtures (web-page.html, word.html), image fixtures (png/jpg/gif/webp/txt), and an at-test-time 21MB PNG generator. 4 paste/drop tests green 40/40 at repeat-each=10, then committed at repeat-each=1. Sent optional N1 reminder about samples\word. |
| P1-11 | Spike: Word converter and fixture generator | done | 0e41470 | https://github.com/stas-comp/comphq/actions/runs/35132364660 | `internal/kb/docx` (Convert, size/depth-limited zip+XML reading, title/heading extraction) and `e2e/fixtures/docx/gen` (sample.docx, googledocs.docx, big-20pages.docx, bad/*, all byte-reproducible — verified locally and in CI). Unit tests green (title/headings, all 6 bad fixtures reject cleanly, zip-bomb memory bound). D-07 recorded. |
| P1-12 | Spike: offline test and upgrade/rollback harness | done | dc2072a | https://github.com/stas-comp/comphq/actions/runs/35136450705 | Added `/__test/egress`, `deploy/test/offline.override.yaml`, `e2e/smoke/{basic,offline}.spec.ts`, and the offline + upgrade/rollback sub-tests in `container-test.sh`. 3 CI iterations to fix real bugs found only on Linux (internal networks block published ports; root-owned files from the offline runner blocking the host's own Playwright run) — all four container sub-tests green, then confirmed again on a full dispatch (go/browser/container/speed/screens all green). D-13 recorded. |
| P1-13 | People: picker, add name, cookie | done | 6eda353 | https://github.com/stas-comp/comphq/actions/runs/35142595282 | `internal/people` (store, picker handlers, identity middleware), `internal/app.OriginCheck`, migrations/people. Also fixed a P1-04 gap: `expectNoSideScroll` was defined but never wired to a test — added `e2e/frame/no-side-scroll.spec.ts` covering the page registry. 6 Go unit tests, 4 Go integration tests, 7 people E2E tests, updated a11y/screens/no-side-scroll for the `who` page — all green locally and in CI. D-14 recorded. |
| P1-14 | Frame, theme, section registry, Coming soon, 404, headers | done | 1ecf894 | https://github.com/stas-comp/comphq/actions/runs/35144794518 | Real sidebar/topbar frame, theme.css (Archivo + Atkinson Hyperlegible Next, both OFL, vendored with checksums), 5 section icons, manifest+icons, section registry (`internal/app.Section`/`Registry`), placeholder sections (briefing/tasks/calendar/kb all "Coming soon"), 404 page, `/`→`/kb` redirect. Generalised the P1-09 vendor-checksum policy test to cover multiple VENDOR.md files. 10 frame E2E tests + 6 a11y + 6 screens + 6 no-side-scroll, all green (contrast included) locally and in CI. D-09, D-15 recorded. |
| P1-15 | Settings shell: People, About, Set up this computer | done | 4e6ccb1 | https://github.com/stas-comp/comphq/actions/runs/35146031344 | `internal/settings` (People rename/remove, About, Setup with Chrome/Brave `--app=` commands built from `r.Host`). Fixed a P1-14 gap: nav "current" marking only matched exact paths, so `/settings/*` sub-pages never highlighted Settings — now prefix-aware. 8 Go unit tests, 5 settings E2E tests (rename, remove, About version, clipboard Copy, gate 1.43 `--app=` window), a11y/screens/no-side-scroll on all 3 settings pages — all green locally and in CI. |
| P1-16 | README part 1 | done | 354c04b | https://github.com/stas-comp/comphq/actions/runs/35146399528 | README.md filled in: Opening, Setting up a computer, Installing on TrueNAS (fixed IP, dataset, snapshots, install via YAML, health check), Updating, Undoing an update; placeholders for Backing up/Restoring/It won't open (P1-37). Added `tools/policy` heading test. |
| P1-17 | Categories and Knowledge Base home | done | ec44449 | https://github.com/stas-comp/comphq/actions/runs/35147752701 | `internal/kb` categories store+handlers (create/rename/reorder/delete with refusal), KB home (tiles + recent-articles query, empty until P1-19), `internal/app/format` date helper. `migrations/kb/0002_categories.sql` creates the full `kb_categories`+`kb_articles` schema now (SPEC B3) so the delete-refusal check has a real table. Added a test-mode-only `/__test/kb/seed-article` route (same pattern as P1-13/P1-06) since there's no publish flow yet. 8 Go unit + 2 HTTP integration + 3 E2E tests, all green locally. |
| P1-18 | Sanitiser and block ids | done | ff2ddef | https://github.com/stas-comp/comphq/actions/runs/35149166405 | `internal/kb/html` (SPEC B4): `Sanitize` (bluemonday structural allowlist, headings remapped to h2/h3 before sanitising, link scheme restricted to http/https/mailto with rel="noopener" added, images dropped unless `src` already starts with `/images/`), `AssignBlocks` (numbers `p h2 h3 li blockquote tr figcaption` in document order with `data-b`, starting at 1; block 0 is reserved for the title per SPEC B3), `BlockTexts` (plain text per block, nested blocks keep their own text separate). Added `github.com/microcosm-cc/bluemonday` and `golang.org/x/net` (3 direct go.mod requirements, within the policy limit of 5); bluemonday's own URL validation couldn't express "keep the `<a>` but drop a bad href" and "drop the whole `<img>` unless local" at once, so link/image rules run as a custom pass after it. 15 Go unit tests (table-driven sanitiser fixtures, idempotency, block numbering incl. nested `li`, block text extraction), all green locally. |
| P1-19 | Article store, publish, read, versions, search rows | done | 6184a5d | https://github.com/stas-comp/comphq/actions/runs/35151018217 | `internal/kb.ArticleStore.Publish` (SPEC B3/B4): one transaction sanitises the body (P1-18), assigns block ids, upserts `kb_articles`, inserts a `kb_article_versions` row (`migrations/kb/0003_article_versions.sql`), and rewrites `kb_search` rows (block 0 = title, 1..n = body blocks). Handlers: `GET /kb/new`, `GET /kb/articles/{id}`, `GET /kb/articles/{id}/edit`, `POST /kb/articles` (one endpoint for both create and edit, via a hidden `id` field), plus `GET /kb/categories/{id}` to browse one category's articles — this also gives the P1-17 home tiles and recent list their destination link, now made clickable. The body field is a plain `<textarea>` stub until the real editor arrives in P1-20. 5 Go unit tests (version numbering, search row replacement on edit, full-transaction rollback on error, empty-title rejection, published-only listing), 2 HTTP integration tests, 4 E2E tests (gate 1.14 publish-appears-in-category, gate 1.13's recent-list half, edit records a new version, axe/no-side-scroll on the article and category pages), all green locally. |
| P1-20 | Editor page: toolbar, publish/cancel, leave warning | done | 881a29e | https://github.com/stas-comp/comphq/actions/runs/35169052361 | Real editor (`web/templates/kb/editor.html`, `web/static/kb/editor.js`, `web/static/kb/messages.js`) built on the P1-09 TipTap bundle, replacing P1-19's plain-textarea stub. Toolbar: two heading sizes, bold, italic, bullets, numbers, link (native prompt), table with add/remove row/column (disabled outside a table, live-updated via `editor.can()`), image (file chooser → local blob preview only; real upload is P1-21's job, and `Sanitize` drops the preview on publish since it isn't under `/images/` yet), Import from Word (disabled with a tooltip until P1-33). Publish/Cancel, a `beforeunload` prompt when dirty, and Cancel confirms "Leave without saving?" first. D-18: the article's initial HTML is handed to the editor via a `<script type="application/json">` data block, not an inline script, since the CSP has no `'unsafe-inline'` for scripts. Deleted the now-redundant P1-09 `editor-spike.spec.ts` (the real editor's own E2E tests supersede its toolbar-command coverage); kept the `/__test/editor` harness since `paste-spike.spec.ts` (P1-10) still uses it. 27 E2E tests (gate 1.15 per-action publish assertions, gate 1.20 dirty/beforeunload/Cancel, keyboard-only publish, CSP-violation check, plus P1-19's updated article/category tests against the real editor), a11y/screens/no-side-scroll on `/kb/new` all green locally. 3 CI iterations to fix flakes seen only on CI's Linux/headless Chromium: toggling a heading/list swaps the paragraph DOM node for a new one, and typing immediately after the toolbar click could race that swap and lose the first few keystrokes — fixed by clicking the actual `.ProseMirror` node (not its wrapper), waiting for it to be focused, and waiting for the new block node to exist before typing into it. |
| P1-21 | Image store, upload, paste and drop | done | 2c58016 | https://github.com/stas-comp/comphq/actions/runs/35170233363 | `internal/kb/images.Store` (SPEC B3/B4): content-sniffed (JPEG/PNG/GIF/WebP only, no SVG; `http.DetectContentType` plus a manual WebP signature check since Go's sniffer doesn't recognise it), SHA-256-named, temp-file-then-rename write, duplicate uploads collapse to the same file and an idempotent `INSERT ... ON CONFLICT DO NOTHING`. `migrations/kb/0004_images.sql`. `POST /kb/images` (raw body, not multipart), `GET /images/{filename}` (Content-Type from the stored row, nosniff, immutable caching). `app.Server` now carries `DataDir` (threaded from `cmd/comphq/main.go`'s config) so the image store knows where to write. `editor.js`: FileHandler `onPaste`/`onDrop` upload-then-insert; the toolbar's Image button now uploads for real instead of P1-20's local blob-preview stub; a `.docx` drop shows a "coming soon" message (real Import from Word is P1-33); upload failures show the server's plain-text error in a new `#editor-message` element. 8 Go unit tests (sniffing each type, SVG and renamed-.txt refusal, duplicate collapse), 4 HTTP integration tests (20MB boundary, headers, origin check), 9 E2E tests (gate 1.16 paste-and-publish-locally-served-and-on-disk, gate 1.17 drop each of the 4 types plus oversize/wrong-type/`.docx` messages), all green locally (repeat-each=3, no flakes). Fixed a real bug the E2E tests caught: the oversize-upload handler wrote its 400 response without draining the rest of the request body, so the browser's `fetch()` saw a broken connection instead of the response. |
| P1-22 | Paste clean-up for web pages and Word | done | d38c808 | https://github.com/stas-comp/comphq/actions/runs/35171229217 | `editor.js`'s new `transformPastedHTML` hook rewrites `img[src^="file:"]` (Word's clipboard pictures) into `<div data-missing-kind="picture" data-missing-src="...">` on the raw pasted HTML string, and shows the exact SPEC gate 1.18 message; a new `missingPicture` TipTap node (`scripts/vendor-editor/entry.js`, re-vendored, checksum updated) makes that div a real schema node so it survives both re-parsing and `getHTML()` instead of being dropped as an unrecognised element. Red dashed box styling in `editor.css`, shown both in the editor and in a published article (`.kb-article-body`). `data:`/`http(s)` images are left untouched at paste time — publishing copies those onto the NAS in P1-23; today's sanitiser (P1-18) correctly still drops them at publish since they aren't under `/images/` yet. Added an `<img>` to `e2e/fixtures/paste/web-page.html` so gate 1.18's "images kept" is provable. 1 Go unit test (the sanitiser strips the same web-page fixture's style/class/font, on the raw fixture bytes — `<b>`/`<span>` normalise to `<strong>` etc. in the editor, not in the sanitiser alone, so that part is proven by the E2E test instead), 2 E2E tests (web-page paste: headings/bold/lists/links/table/image kept in the editor, then publish strips all presentation and the not-yet-local image is correctly dropped; Word paste: placeholder + exact message, survives publish). Scoped `word.html` to what SPEC B7's own verification text asks for (allowed elements kept, style/font/class stripped, `file:` pictures placeholder'd) rather than the gate list's fuller prose, since Word's real clipboard HTML fakes lists via `mso-list` CSS on plain paragraphs, not real `<ol>/<li>` markup — reconstructing that is a separate, much larger problem the plan doesn't ask this task to solve. |
| P1-23 | External images copied on publish | done | e991f3e | https://github.com/stas-comp/comphq/actions/runs/35172386081 | `internal/kb/images/fetch.go`: `FetchAndStore` (SSRF-safe: a `net.Dialer.Control` hook refuses loopback/private/link-local/multicast/unspecified addresses checked against the actually-dialed IP, not just the URL's hostname, defeating DNS rebinding; 10s timeout as a `var` so tests can shrink it; max 3 redirects; http/https only) and `StoreDataURL` (`data:` decode), both funnelling into `Store.Save` so the 20MB cap and content sniffing apply uniformly regardless of where the bytes came from. `internal/kb/html.RewriteExternalImages` (pure function, no HTTP/DB knowledge) walks a body for `<img>` tags not already under `/images/`, calling a caller-supplied `fetch` callback; `ArticleStore.Publish` now runs this before `Sanitize`, using `images.Store` as the callback, and threads any failures back via a new (not persisted) `Article.ImageFailures` field. `handleSaveArticle` renders the article directly (200) with the failures listed when there are any, instead of the usual redirect, since the text is still saved either way (SPEC gate 1.19). A test-only `COMPHQ_TEST_ALLOW_FETCH_HOST` env var (honoured only under `COMPHQ_TEST_MODE=1`) lets exactly one loopback address through the SSRF guard, for the Playwright-started stub image server; `e2e/helpers/fixtures.ts` now reserves a second per-worker port for this and exposes it as `server.stubImageHost`. 15 Go unit tests (IP classification, data: decoding, the pure rewrite function's success/failure/skip-already-local/document-order cases), 3 HTTP integration tests (stub success rewrites to `/images/`, `data:` stored, an unreachable image is placeholder'd with the original URL named while the rest of the text is saved) plus 8 more directly on `images.Store` (loopback refused by default, the test override works, a redirect to a *different*, non-allow-listed loopback address is still refused, too-many-redirects, timeout, oversize, non-image content), 2 E2E tests (gate 1.19 both halves), all green locally (repeat-each=5 on the E2E pair, no flakes). |
| P1-24 | History, restore, archive | done | c621339 | https://github.com/stas-comp/comphq/actions/runs/35174198643 | `internal/kb/history.go`: `History` (every `kb_article_versions` row, newest first, editor name joined live so a rename/removal in Settings → People is reflected without touching old rows — SPEC gate 1.05), `Version` (one version's full content, unchanged by viewing it), `Restore` (a new version_no with the old content, action `restored` with `restored_from_version` set, search rows rewritten only if currently published), `Archive`/`Unarchive` (a status change recorded as its own history entry at the *same* version_no since content didn't change; archive deletes search rows, unarchive rewrites them). Routes: `GET .../history`, `GET .../versions/{n}`, `POST .../versions/{n}/restore`, `POST .../archive`, `POST .../unarchive`, `GET /kb/archived`. Refactored `Publish`'s search-row-writing into a shared `rewriteSearchRows` helper reused by Restore/Unarchive. Extracted a shared `attrValue` helper in `internal/kb/html` while here (three identical attribute-read loops). 10 Go unit tests, 4 HTTP integration tests including a route-table check that DELETE never succeeds against any article-related path (SPEC gate 1.25 — Go 1.22's enhanced ServeMux answers 405 automatically for a registered path given an unregistered method), 5 E2E tests (gate 1.22 newest-first with name/time, 1.23 old version displayed and restored with the restorer's name recorded, 1.24 archive/unarchive round-trip through the category and Archived list, 1.05 rename/remove propagation into History, plus standard page checks for History and a version view), all green locally (repeat-each=3 on the E2E suite, no flakes). One more CI-only recurrence of the P1-20 editor flake (this time only the very first keystroke, on the large-heading test) got through local repeat-each testing again — waiting for the new node to exist wasn't quite enough, since the browser's selection can still lag a moment behind; fixed by clicking the new node directly (a real click places the cursor synchronously) in the four block-toggle toolbar tests. |
| P1-25 | Edit conflicts | done | e20681a | https://github.com/stas-comp/comphq/actions/runs/35175126046 | `ArticleInput` gained `ExpectedVersion`/`Override`; `Publish`'s edit branch compares `ExpectedVersion` against the article's current `version_no` inside the transaction and returns `ErrVersionConflict` on a stale mismatch (skipped entirely when `Override` is set — the next version_no is still computed fresh from the DB, so an overriding publish always lands correctly regardless of how stale the submitted number was). `handleSaveArticle` reads the editor's hidden `version_no`/`override` fields; on conflict it re-renders the editor (status 200, no redirect) with the exact posted content, the exact SPEC message, and `.Conflict = true`, which swaps the template's Publish button for "Publish mine anyway" (a hidden `override=1` field on the same form). Updated every existing edit-publishing test/call site (Go and HTTP) to pass the correct expected version, since an edit with no `version_no` now looks exactly like a stale one. 2 new Go unit tests, 1 new HTTP integration test (conflict returns the editor, not a redirect), 1 E2E test with two browser contexts per SPEC B7 1.20–1.21 (exact message, text kept, override saves, both versions in History), all green locally (repeat-each=5, no flakes). This closes Milestone 1.5. |
| P1-26 | Search: live panel, results page, scroll-to-passage | done | 3b1a3f0 | https://github.com/stas-comp/comphq/actions/runs/35189230009 | `internal/kb/search.HighlightMatches` (SPEC B4) returns every block of an article a query matches, keyed by block id — including block 0, the title, since bm25's 10x title weighting can make the title an article's single best-ranked block. `internal/kb/html.AddHighlightData` sets a `data-hl` attribute (the block's highlighted HTML) on each matched block by its `data-b` id. New `GET /kb/search` (full results page, `web/templates/kb/search.html`) and `GET /kb/search.json` (`internal/kb/search_handlers.go`) routes share a `searchResults` helper; `renderArticlePage` marks matches when the article page is reached with `?q=`. `web/static/kb/search.js` (loaded globally via `layout.html`) is the live panel on the topbar search box: 250ms debounce, fetches `search.json`, renders results with title/category/highlighted snippet, arrow-key navigation, click-outside/Escape to close, Enter with no selection falls through to the form's own GET so the full results page needs no JS. `web/static/kb/highlight.js` (loaded on the article page) swaps in each `data-hl` block's highlighted HTML, scrolls to the one named by `window.location.hash` (the server never sees the fragment — SPEC B4), and adds a "Clear highlights" button. D-23 recorded (the title/fragment design). A real bug caught by manual browser verification, not by a test written before it: the first `HighlightMatches` only queried body blocks, so a title-match result linking to `#b-0` highlighted nothing — fixed by also querying column 0 for block_id=0, adding `data-b="0"`/`data-hl` to the article `<h1>`, and widening `highlight.js`'s query scope from `.kb-article-body` to `document`. A second real bug caught by the new a11y test: the live panel's `role="listbox"` div had no accessible name — fixed with `aria-label="Search results"`. 5 Go unit tests (`HighlightMatches` body/title/symbol-fuzz coverage), 2 HTTP integration tests (`search.json`'s shape and `encoding/json`'s own HTML-escaping of a title/category with special characters; archived articles excluded), 8 E2E tests (`e2e/kb/search.spec.ts`: gate 1.28's exact scroll-and-highlight script, gate 1.26 title-outranks-body ordering and the 2-letter minimum, gate 1.27 title/category/highlighted-snippet shown, gate 1.29 stemming and prefix matching, gate 1.30 publish/edit reflected in the very next search, gate 1.31 archived exclusion, gate 1.32 no-match wording and symbol-fuzz with no console/page errors, standard page checks for `/kb/search` plus axe with the panel open), `/kb/search` added to the shared page registry (a11y/screens/no-side-scroll), all green locally (`go vet`, `gofmt`, `go test ./...`, `go test -tags integration ./...`, `npm run check`, `npm run test:screens`). |
| P1-27 | Test-library seeder and Knowledge Base speed tests | done | bff8a03 | https://github.com/stas-comp/comphq/actions/runs/35190746743 | `e2e/seed` (`package main`): a deterministic Go seeder using `internal/kb`/`internal/kb/images`/`internal/people`'s store types directly (SPEC gate 1.33) — `-data -articles -words -categories -images` flags, a fixed `math/rand` seed so reruns are byte-identical, 20 genuinely distinct generated PNGs reused across articles, every 10th article gets a second version (real History content) and every 25th ends up archived (real Archived content). It always finishes and exits before the real comphq binary opens the same `comphq.db` (D-24 — no concurrent-writer concerns). `e2e/helpers/global-setup.ts` now also builds a `comphq-seed-e2e` binary alongside the app binary, exposed as `COMPHQ_SEED_BINARY`. `e2e/speed/fixtures.ts` is a speed-only worker-scoped fixture that runs the seed binary once into a fresh data dir, then starts the real server against it — replacing the placeholder `server` fixture for this project only; `playwright.config.ts`'s `speed` project now runs with `workers: 1` so "seeds once" is actually once, not once per parallel shard. `e2e/speed/kb.speed.spec.ts` replaces P1-06's placeholder: median-of-5 navigation-to-`data-ready` for KB home/a category/an article/the editor/History/Archived/the search results page (≤1,500 ms each), median-of-5 last-keystroke-to-panel-render (≤1,000 ms), and p95-of-50 `search.json` queries against real vocabulary words (≤200 ms) — all against the 500-article seeded library. All three green locally (`npx playwright test --project=speed`); the full `npm run check` (build, unit, integration, policy, e2e, a11y) and `npm run test:screens` are unaffected and still green. |
| P1-28 | Internal release v0.0.2 | done | cd58289 | https://github.com/stas-comp/comphq/actions/runs/35194166007 | `e2e/smoke/basic.spec.ts`'s `@seed`/`@verify`/`@verify-prev` are real now (SPEC gate 1.42): a person, a fixed-title category and article (D-25 explains why the article/category names are fixed literals but the person isn't), a pasted image, and an edit that appends rather than replaces so the image and both text versions survive into the latest version. `playwright.config.ts`'s `smoke` project now runs with `workers: 1` (same reason as `speed`'s, D-24) since these three tests only make sense run back-to-back against the one server `@seed` just wrote to. Two real bugs surfaced by actually running this for the first time: `deploy/test/container-test.sh` hardcoded the *previous* release's image tag (`IMAGE_REF`) separately from `deploy/truenas.yaml`, so bumping the latter to 0.0.2 made the container job try to pull a real, unpublished tag instead of using the image it had just built — fixed by deriving the tag from `deploy/truenas.yaml` itself (D-26); and `@verify-prev` ran unconditionally in `run_upgrade_rollback_test`'s phase 2, which is fine for a no-op placeholder but fails for real once PREV (still v0.0.1, which predates this harness and never seeds anything) has nothing to verify — fixed by sharing `run_prev_smoke`'s existing "does PREV even have smoke tests" check (D-27). `deploy/truenas.yaml` and `CHANGES.md` bumped to 0.0.2. Full CI dispatch green (run 35193497641: go/browser/container/speed/screens all pass, container's upgrade/rollback phase 1 correctly skips smoke assertions since v0.0.1 predates the harness). Tag `v0.0.2` pushed; Release run 35194166007 green end to end (incl. `container`'s upgrade/rollback against v0.0.1, and `publish`). Verified anonymously: a token request plus a GET on the `0.0.2` manifest returns 200 with no auth header, same method as P1-06's v0.0.1 check. The *next* `[container]` push (any future task) will have v0.0.2 as PREV, at which point phase 1 seeds real data with v0.0.2's own `@seed` and phase 2's `@verify-prev` actually verifies something for the first time — proving gate 1.42 for real, as this task set out to do. |
| P1-29 | Word converter: text, styles, headings, title, revisions, fields | done | 550ab0d | https://github.com/stas-comp/comphq/actions/runs/35196377738 | `internal/kb/docx`'s paragraph model changed from one flat string to `[]segment` (text with resolved bold/italic, or a line break), so `document.go`'s token walker now tracks each `<w:r>`'s own `w:rPr` (bold/italic, `w:rStyle`) and resolves it against `styles.go`'s new `styleSheet.resolveMarks` (paragraph style's top-level `rPr` → character style's, via `w:rStyle` → the run's own direct formatting, each level only overriding what's still unset — SPEC B4 "honouring w:val=0/false and style inheritance"). `w:tab` → a literal space; `w:br` → a real `<br>`, with `type="page"`/`"column"` ignored entirely (not even a space). `w:instrText` is skipped like `w:del`/`w:delText`/`w:moveFrom` already were, so legacy complex-field HYPERLINK URLs never leak into text; `w:ins`/`w:moveTo` need no special handling beyond simply not being in that skip list. `w:sdt`/`w:smartTag`/`w:customXml`/`w:fldSimple` need no special handling either — the flat token-walk finds a `<w:p>` by its own start/end tokens regardless of what wraps it. `Result` gained `Notes []string`: one entry per `w:commentReference`/`w:footnoteReference`/`w:endnoteReference` found in the body, plus one per header/footer relationship in `document.xml.rels` (`docx.go`'s new `headerFooterNotes`) — comments/footnotes/endnotes/headers/footers are never rendered, only counted (SPEC B4). D-28: building the segment model surfaced a real bug in the P1-11 fixture generator — `image()`/`chartFrame()`/`smartArtWithFallback()`/`drawnShapeNoText()` return a bare `<w:r>`, and several `sample.go`/`googledocs.go`/`big.go` call sites spliced it directly into the body as an invalid sibling of `<w:p>`, which the old flat-string parser silently discarded entirely (a real, confirmed data-loss bug caught by actually inspecting the converted output, not by a test written in advance) — fixed by wrapping every such call in `p("", ...)`, plus a defensive parser change to ignore (not silently lose) a `<w:r>` found outside any `<w:p>`. 15 Go unit tests: 6 new narrow fixtures built inline in `docx_test.go` via a new shared `buildDocx` helper (bold-off-overrides-a-bold-style, a 2-hop `basedOn` chain resolving a heading level, tracked insert/delete, descending into an `w:sdt`, title from the first heading 1, tab/break handling per type, an endnote reference), plus 2 new assertions against the existing `sample.docx` fixture (run marks/tracked-changes/field-instruction-skip together, and the exact notes count) — all green locally, `npm run fixtures:docx` reproduces the regenerated `sample.docx`/`googledocs.docx`/`big-20pages.docx`/`bad/truncated.docx` byte-identically on a second run. |
| P1-30 | Word converter: lists and links | done | f1bb051 | https://github.com/stas-comp/comphq/actions/runs/35197905825 | `internal/kb/docx`'s `segment` gained `href`, resolved via a `hrefStack` in `document.go`'s token walker: `w:hyperlink` pushes its external relationship's target (resolved from a new `pkgReader.hyperlinkRelationships`, added to `docx.go`'s `Convert`) or `""` for an internal-anchor-only link; a complex field (`w:fldChar` begin/separate/end plus the `w:instrText` in between, captured into its own buffer so it never reaches the visible text) or a simple field (`w:fldSimple`'s `w:instr` attribute) pushes the URL a new `parseHyperlinkFieldInstr` extracts, only when it's a recognised `http(s)`/`mailto` target — an internal `\l` bookmark jump correctly resolves to no link, keeping its text (SPEC B4). Rendering (`result.go`) groups segments by `href` before grouping by bold/italic, so a link spanning differently-formatted runs becomes one `<a>`, not several. New `numbering.go` (`numberingSheet`) parses `numbering.xml`'s `abstractNum`/`num` structure to resolve a `(numId, ilvl)` pair to bullet-vs-other format; `paragraph` gained `directNumID`/`directIlvl`, and a new `styleSheet.numbering` resolves numbering through a style's `basedOn` chain when a paragraph has no direct `w:numPr` of its own (D-29 covers the segment-as-attribute and list-grouping design together). `buildResult` groups consecutive same-numId paragraphs (empty ones filtered first, so a blank line doesn't split a list) and `renderList` builds the nested `ul`/`ol` structure via a small level stack. 6 new Go unit tests, all built inline via `docx_test.go`'s `buildDocx` family (now split into `buildDocx`/`buildDocxWithNumbering`/`buildDocxWithHyperlinkRels`, sharing a common `buildDocxFull`): three nested levels mixing bullet and number formats, a list interrupted by an ordinary paragraph starting a fresh one, a list defined entirely via paragraph style, a complex-field hyperlink spanning a bold run and a plain run as one anchor, a `mailto:` link, and an internal anchor keeping its text with no link — all green locally alongside the full existing suite, and `sample.docx`'s own two lists and two hyperlinks (already in the fixture since P1-11) now render as real nested lists and anchors rather than plain paragraphs. |
| P1-31 | Word converter: tables, text boxes, alternate content | done | 63d18ad | https://github.com/stas-comp/comphq/actions/runs/35213167850 | `internal/kb/docx` gained a `block` concept (paragraph or table) since a table's row/cell nesting doesn't fit the flat paragraph model P1-29/30 built — `document.go`'s renamed `parseBlocks` special-cases `<w:tbl>`, decoding it whole via `encoding/xml.DecodeElement` into new `tables.go` structs, with each cell's content captured as raw XML (`xml:",innerxml"`) and fed straight back into `parseBlocks` (wrapped with every namespace declaration this converter reads, since a captured fragment carries none of its own) — so cells get the exact same marks/links/list handling as any paragraph, `w:tblHeader` rows become `<th>`, `w:gridSpan` becomes `colspan`, and `w:vMerge` restart/continue becomes a `rowspan` computed per starting column (a "continue" cell is dropped from rendering entirely). A table nested in a cell decodes into its own `tableBlock`, then `flattenBlocksToParagraphs` splices its cells' paragraphs into the outer cell in reading order (SPEC B4's "flattened into paragraphs"), with no special-casing needed. `w:txbxContent` is decoded the same way and queued until the anchoring paragraph closes, landing right after it. `mc:Choice` is now explicitly skipped (reusing the existing `skipDepth` mechanism) rather than accidentally-harmless-so-far, since this converter understands none of its `Requires` extensions and always prefers `mc:Fallback`; `skipDepth == 0` also gates whether a decoded table/text-box is actually kept, catching the case D-30 describes — the same text box present in both a Choice and its Fallback, which would otherwise be counted twice. 3 new Go unit tests: a 3×3 table combining a header row, a horizontal merge and a vertical merge (exact colspan/rowspan asserted), a table nested inside a cell flattening correctly, and a text box duplicated across `mc:Choice`/`mc:Fallback` appearing exactly once — all green locally alongside the full existing suite, and `sample.docx`'s own table (already in the fixture since P1-11) now renders as a real `<table>` with correct rowspan instead of a flat run of paragraphs. |
| P1-32 | Word converter: pictures, unsupported items, limits, unreadable files | done | f7d8db1 | https://github.com/stas-comp/comphq/actions/runs/35215644148 | `internal/kb/docx` gained real pictures and unsupported-item placeholders. New `images.go`: `mediaRelationships` reads image-type relationships (external ones flagged, never resolved to a path; internal ones resolved via a new path-traversal-safe `resolveRelativeTarget` using `path.Join`/`Clean`, refusing anything that climbs above the relationships file's own directory — SPEC "resolve only inside the zip"), and `resolveImage` reads (via a new `readImagePart`, generalising `readPart` to a caller-supplied byte limit so the 20 MB per-image cap composes with the existing 300 MB whole-document one) and sniffs each one (JPEG/PNG/GIF/WebP, the same check `internal/kb/images.Store` uses, re-implemented rather than imported to keep this package dependency-free) — all computed once in `docx.go`'s `Convert` into a plain `relID -> resolvedImage` map, so `document.go`'s token walker stays pure (no I/O). A new `docCtx` (replacing separate `styles`/`hyperlinkRels` parameters threaded through `parseBlocks`/`buildTableBlock`) carries that map plus the running `[]Image`/token-counter state a whole `Convert()` call accumulates across every recursive re-parse. `<w:drawing>` (`wp:inline`/`wp:anchor`, alt from `wp:docPr descr`/`title`, classified by `graphicData`'s `uri` into picture/chart/diagram/anything-else), `<w:pict>` and `<w:object>` (both scanned for a VML `v:imagedata` or `w:txbxContent` anywhere in their subtree via a small dedicated depth-agnostic scanner, since VML's wrapping shape element varies) and `<m:oMath>` (consumed whole so its `m:r`/`m:t` — sharing local names with `w:r`/`w:t` — never leak text) are all decoded via `encoding/xml.DecodeElement` the same way tables and text boxes already are, each producing a new `segMedia` segment kind: either a real `<img src="cid:...">` (the token the P1-33 handler replaces once it stores the bytes) or a `<div data-missing-kind="picture\|chart\|diagram\|shape\|equation\|object">` placeholder, both added to `Notes` too. `mc:Choice` is gated the same way tables/text boxes already were (`skipDepth == 0`), so a picture or table present in both AlternateContent branches is never double-counted. D-31: this surfaced a real bug — `paragraph.hasContent()` had to become its own check separate from `plainText()`, since a picture-only paragraph has no text at all and the existing "drop empty paragraphs" rule was silently eating every image. 15 new Go unit tests (a DrawingML picture with alt text, a VML picture, an external target never fetched, an unsupported format, an oversize image, a path-traversal relationship target actually placed where a naive resolution would wrongly find it, a bare diagram placeholder, an object with and without a cached image) plus a `FuzzConvert` seeded from every committed fixture (run 30s locally with `-fuzz`, no crashes, committed as a plain seed-corpus test) and 2 rewritten assertions against `sample.docx` (exact notes list now including its EMF picture/chart/shape/equation; its two real images render as `<img>` with correct alt/bytes) — all green locally alongside the full existing suite. This closes Milestone 1.6's Word converter core (P1-29–P1-32); P1-33 wires it into the editor. |
| P1-33 | Import from Word in the editor | done | 66b9637 | https://github.com/stas-comp/comphq/actions/runs/35217753981 | New `POST /kb/import/docx` (`internal/kb/import_docx_handlers.go`, multipart, one "file" field, origin-checked like every other POST): reads up to 50 MB via `io.LimitReader` (plus an `http.MaxBytesReader` on the request body itself, for defence against a genuinely huge multipart request before parsing even starts), calls `docx.Convert`, stores each extracted image through the same `images.Store.Save` an upload uses, replaces each `cid:...` token in the HTML with the real `/images/<hash>.<ext>` URL, runs the result through the same `html.Sanitize` as publishing (its allowlist already covered colspan/rowspan/alt/data-missing-kind from P1-18, so no changes needed there), and returns `{title, html, notes}` — it never touches `kb_articles`. `docx.ErrUnreadable` returns SPEC gate 1.51's exact message; `docx.ErrTooLarge` or a body over the multipart cap returns "That file is larger than 50 MB." `editor.js`: "Import from Word" is enabled (its file input accepts `.docx`); a dropped or pasted `.docx` now routes to a new `importDocxFile` instead of the old "coming soon" message (now deleted, along with the message constant); a non-empty editor asks "Replace what's in the editor with this document?" first (Cancel changes nothing); on success the title field and editor content are replaced and, if `notes` is non-empty, a summary message like "3 things couldn't be brought in: a chart, a shape, an equation." is shown. D-32: `editor.css`'s single shared `data-missing-kind` message ("Picture not shown — use Import from Word to bring it in.") had to become one rule per kind value, since it was wrong for anything but a missing picture and self-contradictory during an import. 7 new Go integration tests (converts and stores images with files on disk while leaving `kb_articles` untouched, the Google Docs fixture, each `bad/` fixture's exact message, a zip bomb, an oversize file, origin refusal) and a new `e2e/kb/import-word.spec.ts` (6 tests, all green including 3 repeats with no flakiness): `sample.docx` by button and by drop — headings, nested lists, table colspan/rowspan, two real pictures with alt text and files on disk, every unsupported-item placeholder, the title, and the summary message, with no article existing afterward; `googledocs.docx`; importing into an existing article — Cancel changes nothing, confirming replaces it, and the pre-import version still opens and restores after publishing; every `bad/` fixture plus a real 51 MB file (written to a temp path — Playwright's `setFiles` refuses an in-memory buffer that large) leaving the editor's existing content untouched; and `big-20pages.docx` populating the editor in well under the 10 s budget (gate 1.52). Two existing tests updated for the new real behaviour (the "coming soon" drop test now expects the real gate 1.51 message; the disabled-button test is deleted, since the button is enabled now). `samples/word/*.docx` support is wired in as a loop that only generates tests when the owner has actually added files there (currently none). Full `npm run check` plus `npm run test:screens` green. This completes Milestone 1.6 — Import from Word. |
| P1-34 | Nightly backups and banner | done | a794533 | https://github.com/stas-comp/comphq/actions/runs/35220370907 | `internal/db/backup.go`: `DailyBackup`/`PreUpdateBackup` (`VACUUM INTO` a temp name, rename into place, prune to `DailyBackupsToKeep`=30/`PreUpdateBackupsToKeep`=10 by lexicographic filename), `GetBackupStatus`/`SetLastBackupAtForTest`, `HasExistingData` (D-33: gates the pre-update backup off on a fresh install, since `app_meta` — needed to record any result — doesn't exist until the first migration creates it). `internal/db/scheduler.go`: injectable `Clock` (`RealClock` in production), `RunBackupScheduler` runs once at startup if stale by 24h then daily at 03:00 local, computed from `now`'s own date/location so it's correct across a DST transition (tested against `Europe/London`'s spring-forward/fall-back). `internal/db/migrate.go` gained `HasPendingMigrations`. `cmd/comphq/main.go`: loads every section's migrations up front, runs one aggregate `PreUpdateBackup` before applying any of them if something is pending (and `HasExistingData`), then either seeds a fresh `last_backup_at` (`COMPHQ_TEST_MODE=1`, D-33) or starts the real scheduler as a goroutine. `internal/app/frame.go`'s `frameData` gained `ShowBackupBanner` (`db.GetBackupStatus(...).Stale`, >48h or never run), rendered in `web/templates/app/layout.html` as `.backup-banner` above the top bar; a new test-mode-only `POST /__test/backups/set-last-backup-at` (`days_ago` form value) lets E2E move the timestamp. New Settings → Backups page (`internal/settings/backups.go`, `/settings/backups`, added to the subnav and `e2e/helpers/page-registry.ts`) shows the last backup time (`internal/app/format.DateTime`) and any failure message. 24 new Go unit tests (daily overwrite-same-day and prune-oldest, pre-update's separate dir/retention, a later failed attempt not moving `last_backup_at` back, stale/stale24h thresholds, `HasPendingMigrations`, `HasExistingData` on fresh vs. migrated DBs, the scheduler's immediate-run-if-stale/skip-if-fresh behaviour via a clock whose `After` never fires, DST-safe next-run timing) plus a fix to a real crash `-tags integration` caught first try: the pre-update backup ran before any migration on a genuinely fresh `cmd/comphq` binary and crashed trying to write `app_meta`, which didn't exist yet (D-33's `HasExistingData` fix). 3 new E2E tests (`e2e/settings/backups.spec.ts`: no banner fresh, banner on every registered route when stale via the test-mode helper then gone again once reset, Settings → Backups shows a time and no failure message) plus the new page added to the shared a11y/screens/no-side-scroll registry. Full `npm run check` plus `test:screens` green locally. |
| P1-35 | Export everything | done | 0965a79, 2b64951 | https://github.com/stas-comp/comphq/actions/runs/35223077915 | `internal/kb/export.go`: `ArticleStore.ListForExport` groups published articles by category in the categories' own `sort_order` (omitting categories with none) and lists archived articles separately (SPEC B5: archived ones live in one flat `articles/_Archived/` folder, not nested under their original category); `BuildExport(categories, archived)` renders `index.html` (a contents page linking every article, the Archived section clearly marked) plus one self-contained page per article — inline CSS targeting bare tags (Sanitize's allowlist never keeps a class/id), `<img>` srcs rewritten from `/images/<hash>.<ext>` to `../../images/<hash>.<ext>` via a new `kbhtml.RewriteImageSrcs` — and returns every original image src referenced, so the caller knows what to copy. `sanitizeFilename` (forbidden chars `<>:"/\|?*` and control characters replaced, whitespace collapsed, trailing dots/spaces trimmed, Windows reserved device names suffixed, length capped at 100 runes) plus a per-directory `filenameAllocator` giving `" (2)"`, `" (3)"`... suffixes to same-directory duplicates. `internal/settings/export.go`: `GET /settings/export` (added to the subnav and `Handlers`, which now also builds a `kb.ArticleStore`/`images.Store` the same way `kb.Section` does) streams `comphq-export-YYYY-MM-DD.zip` — every `BuildExport` file, each referenced image read from `images.Store.Path` (a missing file is skipped rather than failing the whole export; Content check in P1-36 is where that gets fixed), and a fresh `comphq.db` via `VACUUM INTO` a temp file. New `e2e/unzip` Go helper (built once in global setup alongside the existing seed binary) extracts a downloaded zip to real files, since the gate 1.34 E2E check needs an actual `file://` URL, not zip entries. 13 new Go unit tests (the filename-sanitising table including reserved names/truncation/trailing-dot edge cases, the allocator's dedup, `BuildExport` grouping/omitting-empty-categories/rewriting-image-srcs, `ListForExport`'s category ordering and archived separation) plus 2 HTTP integration tests (the zip's exact file list and comphq.db's SQLite header, and that the route needs a signed-in person like every other page) and a new E2E test (`e2e/settings/export.spec.ts`: publishes an article with a real pasted image plus an archived one, downloads and unzips the export, opens `index.html` under a genuinely **offline** browser context, follows links to both articles, asserts the image's `naturalWidth > 0`, and asserts zero failed requests). Full `npm run check` plus `test:screens` green locally; the first push's CI caught a genuine, unrelated flake in P1-34's own `TestRunBackupSchedulerRunsImmediatelyWhenStale` on Linux (it polled for the backup file rather than the recorded status, so it could win a narrow race against `backupAndRecord`'s two-step write) — fixed in a follow-up commit, not part of P1-35's own change. |
| P1-36 | Content check | done | 0187d17 | https://github.com/stas-comp/comphq/actions/runs/35243367050 | `internal/kb/html/content_check.go`: `HasUnresolvedContent(bodyHTML)` walks a stored body for anything Content check should flag (SPEC gates 1.36, 1.49) — a `data-missing-src`/`data-missing-kind` placeholder (a failed external-image fetch, or any unsupported Word-import item still in place) or an `<img>` whose src isn't under `/images/` (Sanitize already prevents this on every real path; checked again here only defensively). `internal/kb/content_check.go`: `ArticleStore.ListContentCheck` scans every published or archived article's `body_html` through it. `internal/settings/content_check.go`: new `GET /settings/content-check` page (added to the subnav in SPEC A9's order — People, Export, Backups, Content check, Set up this computer, About) showing the count (`#content-check-count`) and a link to each flagged article. 5 new Go unit tests (`HasUnresolvedContent`'s clean/missing-src/missing-kind/foreign-img cases) plus 3 more (`ListContentCheck` lists published and archived articles with an issue but not clean ones, an empty library returns none, and republishing with the placeholder removed drops the article from the list — SPEC B7: "until the boxes are removed... Content check lists the article"). New E2E (`e2e/settings/content-check.spec.ts`, 3 tests): a clean article never appears; an article with a genuinely unreachable external image is listed (reusing the same unreachable-image technique as `e2e/kb/external-images.spec.ts`); importing `sample.docx` (real chart placeholder) and publishing it is listed, then editing it to replace the whole body and republishing makes it disappear. The exact *count* (0, N) is asserted only at the Go level, against an isolated in-memory database — E2E assertions are scoped to each test's own uniquely named article instead, since every spec file in a CI run shares one server per worker and Content check scans the whole library; asserting an absolute total there would be fragile against whatever else that worker happened to run first (matching how `e2e/kb/categories.spec.ts`'s tile-count test already scopes to its own category rather than a sitewide total). Full `npm run check` plus `test:screens` green locally (one confirmed-unrelated flake, `e2e/kb/search.spec.ts`'s gate 1.29 test, seen once under full parallel load and reproduced as stable in isolation — not touched by this task). |
| P1-37 | README part 2 | done | 5a3adb6 | https://github.com/stas-comp/comphq/actions/runs/35244624997 | Filled in `README.md`'s three P1-16 placeholders and added two new sections plus a summary table (SPEC gate 1.44, B8 rule 7, A13). **Backing up**: TrueNAS snapshots (already covered) plus the P1-34 nightly backup and its Settings → Backups page/banner, plus a click-by-click walkthrough of the P1-35 Export everything download as a second, owner-triggered safety net. **Restoring**: History → Restore for a single bad edit (no NAS access needed), TrueNAS snapshot rollback for anything bigger, and one sentence on why the `backups/daily`/`backups/pre-update` folders exist (for whoever helps, not for routine owner use). **It won't open — what now?**: check Apps status, restart, check the version (cross-referencing Undoing an update), roll back a snapshot. New **Moving your HelpScout articles** (copy an article's content in the browser, paste into a new Comp HQ article, publish, then Settings → Content check should reach 0 — P1-36) and **Word samples** (the `samples\word` folder) sections. New **Your hands-on moments** table mapping every SPEC A13 row to where its steps live — the two one-time GitHub steps (sign-in before building, making the package public after the first release) are noted as handled live with the build agent rather than given their own README walkthrough, since neither is something the owner does unprompted from a written procedure. Extended `tools/policy`'s `TestReadmeHasRequiredHeadings` with the six new headings. Full `npm run check` green locally (no app code changed — README and one policy test only). |
| P1-38 | Phase 1 hardening and full gate sweep | done | 7e3b4da, da98cd1, aa219a1 | https://github.com/stas-comp/comphq/actions/runs/35250669689 (full dispatch; two earlier full dispatches, 35247465972 and 35248677291, caught and were fixed by da98cd1/aa219a1 — see D-37) | `deploy/test/container-test.sh`: the offline sub-test (step 5, gate 1.41) now runs the full `e2e` Playwright project (every non-`@fresh` test) against the container over its internal-only network, via a second `runner` invocation, alongside the existing dedicated no-internet/healthz check; a new step 2b/4b runs `e2e/smoke/basic.spec.ts`'s existing `@seed`/`@verify` pair plus a new `e2e/smoke/names.spec.ts` (`@names-seed`/`@names-verify`, distinct tags so the upgrade/rollback sub-test's own grep-filtered runs never pick them up — D-37) right after bring-up and right after recreate, so gate 1.39/1.40's "data intact" is proven two ways: the existing whole-folder checksum, plus real HTTP/UI checks that the article, its image, its history and a person's name all still open correctly (not just that the same bytes are still on disk). Fixed 3 pre-existing E2E tests that would have broken under the new offline BASE_URL run (D-37): `e2e/kb/external-images.spec.ts`'s "reachable" test needs a real local stub server, so it's now tagged `@fresh` (excluded automatically — `playwright.config.ts` already excludes `@fresh` whenever `BASE_URL` is set); its "unreachable" test and my own P1-36 content-check test both used to build their unreachable URL from `server.stubImageHost`, which is empty in `BASE_URL` mode — both now use a new shared `UNREACHABLE_IMAGE_URL` constant (`http://127.0.0.1:1/gone.png`, blocked by the app's own SSRF guard, fast and identical in every environment); `e2e/kb/import-word.spec.ts`'s shared `expectSampleDocxImported` helper's on-disk image check is now skipped when `server.dataDir` is empty (BASE_URL mode), keeping every other gate 1.47 assertion unconditional. New `reports/gates-phase-1.md`: every SPEC B7 gate-range row for gates 1.01–1.52, mapped to its actual test file(s) and representative test name(s), to be filled in against the `gh workflow run ci.yml -f full=true` run recorded below. `e2e/screens/` and the a11y/no-side-scroll registries already cover every Phase 1 page type as of P1-36 (`e2e/helpers/page-registry.ts`), so no further additions were needed there. Full `npm run check` green locally (container tests can't run locally — no Docker on this machine — so `container-test.sh`'s own changes are verified only by the full CI dispatch run itself). |
| P1-39 | Release v0.1.0 and Phase 1 report (STOP S3) | done | c64fd39 (tag v0.1.0) | https://github.com/stas-comp/comphq/actions/runs/35250669689 (pre-tag full dispatch); https://github.com/stas-comp/comphq/actions/runs/35253225925 (release.yml, incl. `publish`) | Version bumped to `0.1.0` in `deploy/truenas.yaml` and `CHANGES.md`. Tag `v0.1.0` pushed at the exact commit the full dispatch validated; `release.yml` green end to end (go/browser/container/speed/publish); anonymous ghcr.io token + manifest GET on `0.1.0` returns 200 with no auth header (same method as P1-06/P1-28). `reports/gates-phase-1.md` rewritten to one row per gate (1.01–1.52: description, actual test file(s)/name(s), result), `reports/phase-1-report.md` written (summary, full checklist, 8 tests-corrected entries, a plain summary of D-01–D-37, screenshot list, version, install steps, all 4 owner checks with what-to-try/what-to-see), and `reports/phase-1/screens/` (14 screenshots) downloaded from the passing full-dispatch run's `screens` artifact. **Deviation from PLAN.md §5.2/§5.3, by explicit owner instruction this session:** rather than sending S3 and stopping to wait for a reply, all four owner checks (O1.1–O1.4) are logged as `pending` below and Phase 2 starts immediately without waiting for owner confirmation. |
| P2-01 | Tasks data, Board columns, add task, cards | done | 876abd5 | https://github.com/stas-comp/comphq/actions/runs/35273037951 | New `internal/tasks` package replacing the P1-08 "Coming soon" stub: `migrations/tasks/0001_tasks.sql` (tasks/task_assignees/task_activity, SPEC B3 — position is unique per stage among visible tasks by application invariant, not a DB constraint, matching kb_categories.sort_order's own precedent). `Store.Create` (SPEC gate 2.02: bottom of the chosen stage, default Ideas/size M, records a `created` activity row, all in one transaction) and `Store.ListBoard`/`Get` (gate 2.01 stage order, gate 2.03 card content, gate 2.08's 14-day Done retention, gate 2.11's removed-assignee marking). New shared `internal/app.Today(testMode)` (SPEC B4's `COMPHQ_TEST_TODAY` override, needed now for the OVERDUE stamp and later for the Briefing) — `tools/policy`'s `TestDeployYAMLRules` extended to also refuse it in `deploy/truenas.yaml`. `GET /tasks/board`, `POST /tasks`; `GET /tasks` redirects to it until My jobs exists (P2-10). New `web/templates/tasks/board.html`/`web/static/tasks/board.css`. A real CSP bug found and fixed by manually exercising the feature in a browser before calling it done (not by any test — none of this session's tests happened to check computed styles): person-colour avatar circles were built with server-rendered inline `style="background-color: hsl(...)"`, which the app's CSP (`default-src 'self'`, no `unsafe-inline` for style-src) silently drops rather than applies — switched to a fixed palette of 12 pre-defined `.task-avatar-N` CSS classes instead, each hue verified (along with every other possible hue, for headroom) to give white text at least WCAG AA's 4.5:1 contrast against every one, with a test guarding the CSS file and the Go palette from drifting apart. New `e2e/helpers/fixtures-today.ts`: a test-scoped (not worker-scoped) server fixture with a settable `COMPHQ_TEST_TODAY`, needed because different date-dependent tests need different "today" values and can't share one worker-wide server — the first user of it here, more extensively by Phase 3's Briefing. 19 new Go unit/integration tests (position-per-stage, creator/activity recording, size/stage validation, board ordering, 14-day retention — including a real bug this session's own tests caught: a task created straight into Done left `done_at` NULL, which the retention query's `done_at < cutoff` then evaluated as SQL's neither-true-nor-false, excluding it from the board entirely; fixed by setting `done_at` at creation time for a task starting in Done, and hardening the query to treat NULL as "never confirmed old enough to exclude" either way), OVERDUE computation, and the avatar contrast/palette guarantees. 5 new E2E tests (column order; title-only add visible in a second browser context after reload; adding directly into a chosen column; a card showing its people and due date; OVERDUE only for an unfinished, past-due task, never a Done one, via the new per-test `COMPHQ_TEST_TODAY` fixture). Removed `/tasks` from gate 1.08's "Coming soon" list (PLAN.md's own rule 2.5 exception). Full `npm run check` plus `test:screens` green locally. |
| P2-02 | Ordering engine and move buttons | done | 50d7984 | https://github.com/stas-comp/comphq/actions/runs/35275018645 | `internal/tasks/order.go`: `insertTask(others, taskID, beforeID, afterID, toBottom)` — a pure function returning the full ordered id list for a stage after inserting one task at the position implied by exactly one of before/after/to_bottom, erroring `ErrNeighborNotFound` if that id isn't actually in the list (PLAN.md P2-02's "before or after a missing id → error"). `Store.Move` (SPEC B4): reads the task's current stage, computes the target stage's new order via `insertTask`, renumbers it (a full 1..n rewrite of the affected stage each time — boards are small, and this can never drift into a duplicate or a gap, matching "Stability: simple code that works all the time"), updates the task's own `stage` column, and — only when the stage actually changed — renumbers the vacated old stage too and records a `moved` activity row; all in one transaction. `POST /tasks/{id}/move` accepts `stage` plus one of `before_id`/`after_id`/`to_bottom`; `ErrTaskNotFound`/`ErrNeighborNotFound` (the board changed under you) re-render the current board rather than an error page. New per-card UI: Move up/Move down buttons (disabled at the top/bottom of their column — the handler computes each card's current neighbour ids once per render) sending the neighbour that was already there, and a "Move to…" `<select>` of the *other* three stages plus a submit button (no `onchange="submit()"`: the app's CSP has no `unsafe-inline` for scripts either, and this needed no JS at all to be fully keyboard-accessible). 17 new Go unit/integration tests (within-stage before/after, across-stage to-bottom, missing-neighbour and missing-task errors, exactly-one-target validation, `moved` activity recorded only on an actual stage change, and — PLAN's "concurrency via two transactions ends consistent" bullet, meaningful here as "a sequence of moves never corrupts positions" rather than literal thread-racing, since the app's single SQLite connection already serialises every write — a multi-move sequence asserting positions stay a contiguous 1..n after each step). 4 new E2E tests (button reorder consistent in a second context; move-to-a-different-column lands at the bottom; keyboard-only move). Full `npm run check` plus `test:screens` green locally. |
| P2-03 | Dragging on the Board | done | 13131e9, 50886ac, e6e3d57 | https://github.com/stas-comp/comphq/actions/runs/35280129306 | SortableJS 1.15.7 vendored (npm devDependency, exact-pinned, copied pre-minified via new `scripts/vendor-sortable.mjs` — nothing to bundle, unlike the TipTap editor's own vendoring step) into `web/static/vendor/sortable/`, recorded in the shared `VENDOR.md` alongside the editor entry. New `web/static/tasks/board.js`: one `Sortable` instance per column (`group: 'tasks'` for cross-column drags, `filter: 'button, select'` so a move button or the "Move to…" select never starts a drag), posting to the existing `/tasks/{id}/move` endpoint on drop (SPEC B4 — the dropped card's new DOM neighbours are exactly the `before_id`/`after_id` the endpoint already expects) and replacing `.task-board` with the server's own freshly-rendered fragment from the response rather than guessing the new state client-side, so positions and disabled move buttons always match what the server actually computed. Manually verified in a browser that SortableJS initialises and the drop round-trip runs with zero CSP violations or console errors — the same bug class (silently-dropped inline styles/handlers) already hit twice this session with avatars and the move-to select, this time clean by construction since nothing here uses an inline handler or style. 2 new E2E tests (drag reorder within a column persists after reload; drag to another column moves it) using a `page.mouse`-driven synthetic drag (SortableJS drives pointer events itself, not the HTML5 Drag and Drop API, so `locator.dragTo` doesn't reliably trigger it) — this needed real debugging to de-flake: an early version checked `response.ok()` after the drop, which is wrong since the move endpoint's success response is a 302 (fixed to check the status code directly); the drag's start point moved to the card's title text specifically, not the card's overall bounding-box centre, since a centre point can fall inside the actions row that's deliberately filtered from starting a drag at all; and `dragOnce` now polls for SortableJS's own `sortable-chosen` class (the signal it actually started a drag) instead of guessing a wall-clock pause, which is correct at any machine speed. First push's CI still failed one of these tests every time despite 50/50 local passes — reproduced locally with `--workers=1` and found to be a plain test-isolation bug, not drag-simulation flakiness: the file's default `server` fixture is worker-scoped and shared across every other board test with no way yet to delete a task (P2-06), so by the time these ran, "doing" already held enough accumulated cards to push the anchor card below the fold, and `boundingBox()` doesn't scroll anything into view — the synthetic drag was moving the mouse over blank page space. Fixed by switching both drag tests to the existing per-test `fixtures-today.ts` server fixture (`@fresh`, the same pattern gate 2.03's OVERDUE test already uses), giving each its own empty board; a bounded retry (up to 4 attempts) around the whole gesture stays as a hedge against ordinary synthetic-input timing noise. 20/20 passed under `--workers=1` (matching CI) after the fix, plus the full local suite at both default and `--workers=1`. D-41 recorded. First push's CI also caught a second, unrelated reproducibility bug: `vendor:editor` and the new `vendor:sortable` both rewrote the shared `VENDOR.md` by dropping their own row and appending it at the end, which is only order-stable with one script touching the file — CI's "editor bundle is reproducible" step re-runs `vendor:editor` alone and moved the editor row back to the end, diffing against the committed file where the sortable row now came after it. Fixed by having both scripts sort all rows before writing, so the file no longer depends on which script ran last (D-42). Full `npm run check` plus `test:screens` green locally. |
| P2-04 | Task details dialog and Activity | done | bc3a894 | https://github.com/stas-comp/comphq/actions/runs/35282609126 | New `GET /tasks/{id}` (a full page, not a client-side dialog — also usable from calendar links later) and `POST /tasks/{id}` (`internal/tasks/details.go`'s `Store.Update`), editing title, notes, size, stage, due date and people in one transaction, each kind of actual change recording its own activity row (`edited`/`due_changed`/`size_changed`/`assigned`/`unassigned`/`moved`) rather than one generic entry — SPEC gate 2.06 and 2.26 both need Activity to name what changed. A stage change from this form has no drop position, so it goes to the bottom of the new stage, matching the "Move to…" button's own default. New `Store.ListActivity` renders each row as one sentence ("Sam moved this to In progress") plus a separately-formatted date, person names resolved live by id the same way KB History already does (a rename shows immediately). Gate 2.11's "removed person still shows, marked" extends to the people multi-select too: a removed-but-still-assigned person is kept as a selected option (labelled "(removed)") so simply re-saving the form without touching people can't silently unassign them, since they'd otherwise never appear as an option to keep selected. Board cards now also show the size label (SPEC gate 2.26) and the title links to the details page (previously nothing linked there at all). Fixed a real, pre-existing gap found while building this: `Store.Move` (P2-02/P2-03, drag-and-drop and the move buttons) never set `done_at` when a card moved into Done, so gate 2.08's 14-day retention could never actually exclude a dragged-in card — only `Create` handled it; both `Move` and the new `Update` now share one `updateDoneAtForStageChange` helper that sets it entering Done and clears it leaving (D-43). Also hit and fixed a real single-connection deadlock while building `ListActivity`: rendering "assigned"/"unassigned" needs a second query (the target person's name), and running it while the first query's `rows` were still open deadlocked outright against the app's `SetMaxOpenConns(1)` — fixed by draining the activity rows fully before rendering any of them (D-44). 22 new Go unit tests (every kind of field change and its activity row, a no-op save recording nothing beyond `created`, stage-change-moves-to-bottom, missing-task and invalid-input errors, activity rendering and live name resolution after a rename, the done_at fix). 4 new E2E tests (editing every field with Activity checked for every message kind including unassign; size shown and changeable on the board card; keyboard-only; standard page/axe/no-side-scroll checks). Full `npm run check` plus `test:screens` green locally. First push's CI hit the same pre-existing, already-documented flake as P1-36 (`e2e/kb/search.spec.ts`'s gate 1.29, unrelated to this task) — reproduced as stable in 5/5 local isolation runs and green on CI rerun. |
| P2-05 | Board filters | done | 0cdc7a2 | https://github.com/stas-comp/comphq/actions/runs/35284339841 | SPEC gate 2.07: a plain GET filter bar on the board (`?mine=1`, `?person=<id>`, `?q=<word>` — the filter state living in the URL, per PLAN, rather than any client-side JS) with an explicit "Filter" submit button (no auto-submit-on-change: the app's CSP has no `unsafe-inline` for scripts, so this needed no JS at all, the same reasoning already used for the "Move to…" select in P2-02). New `Store.ListBoard(ctx, today, BoardFilter{PersonID, Query})`: `PersonID` narrows to tasks that person is assigned to (`mine=1` resolves to the signed-in person before reaching the store — the store only ever knows "filter by this one id", never "mine" as a concept), `Query` matches anywhere in the title case-insensitively (`LOWER(title) LIKE '%'||LOWER(?)||'%'`, the same `LOWER(...) = LOWER(?)` idiom `internal/people` already uses for its own case-insensitive duplicate-name check). "My tasks" takes priority if a person is also somehow selected at the same time, since they're the same underlying filter and the store only takes one id. A "Clear filter" link appears whenever any filter is active. Existing Move-button/drag semantics needed no changes: they already act on named neighbour ids from the *full* stage order (`Store.Move`'s own `stageOrder`), so a filtered view's card still moves to sit correctly next to whichever neighbour is visible, even with other cards hidden between them. 2 new Go unit tests (person filter, case-insensitive title filter). 1 new E2E test covering all three filters plus Clear, using a second signed-in person to prove the person filter actually excludes someone else's task, not just an unassigned one. Full `npm run check` plus `test:screens` green locally. |
| P2-06 | Finished tasks and Removed tasks | done | d33fdea | https://github.com/stas-comp/comphq/actions/runs/35297243956 | SPEC gate 2.08: `GET /tasks/finished` lists Done tasks that have aged off the board (`removed_at IS NULL AND stage='done' AND done_at < cutoff`, newest-finished first), each with a Reopen button (`POST /tasks/{id}/reopen`) that returns it to the bottom of To do, clears `done_at`, and records a `reopened` activity. SPEC gate 2.09: `POST /tasks/{id}/remove` (from the task details page) sets `removed_at` without ever deleting the row, renumbering the stage it leaves; `GET /tasks/removed` lists them newest-removed first, each with Restore (`POST /tasks/{id}/restore`, landing at the bottom of the stage it was removed from — not its old position, which Remove's own renumbering may have since reassigned). New shared `Store.listTasks` helper backs both new list queries (`ListBoard` keeps its own copy since it alone needs `Overdue` computed). A route-table integration test (mirroring KB's own gate-1.25 precedent exactly) proves DELETE never succeeds on any task-related path. E2E-testing the 14-day transition itself needed a new test-only route, `POST /__test/tasks/set-done-at` (days_ago, same convention as the existing backups one) — added directly in `internal/app/server.go` against raw SQL, not the tasks package's own Store, for the same import-cycle reason `handleTestSeedKBArticle` already does the same for `kb_articles` (D-14, D-45). 8 new Go unit tests (the exact 14-day boundary at ±1 minute against a fixed clock, Finished ordering, Reopen's position/done_at/activity bookkeeping, Remove's renumbering leaving no gap, Restore's round trip, missing-task errors for all three) plus 3 new HTTP integration tests (Reopen/Remove/Restore over real requests, and the DELETE route-table test). 3 new E2E tests (the full aged-task → Finished → Reopen → back-in-To-do flow using the new test-only route; the full Remove → Removed → Restore flow, needing no date tricks at all; standard page/axe/no-side-scroll checks for both new pages). Full `npm run check` plus `test:screens` green locally. |
| P2-07 | Refresh without live sockets; removed people on tasks | done | d97ada7 | https://github.com/stas-comp/comphq/actions/runs/35298771538 | SPEC gate 2.10: `GET /tasks/version` answers a change counter (`app_meta` key `tasks_version`) bumped inside the same transaction as every write (`Create`/`Update`/`Move`/`Reopen`/`Remove`/`Restore`, via one shared `bumpTasksVersion` helper), so it only ever advances when a write actually commits. New shared `web/static/app/refresh.js` (loaded globally via `layout.html`, like `kb/search.js` already is): opts in per page via `data-refresh-version-url`/`data-refresh-fragment-selector` on one marker element (the Board's own `.task-board` div, for now) — polls every 60s while the tab is visible, and immediately on `focus`/`visibilitychange`, re-fetching the page and swapping in just that element rather than a full reload. Never swaps while `document.body`'s `data-refresh-busy` attribute is set (board.js sets it for its own SortableJS drag, the only busy state the Board has today); instead a shared "Updated — refresh" button (new in `layout.html`, hidden until needed) appears, and clicking it applies the pending update. `board.js` also re-runs `initSortable()` after a refresh-triggered swap, the same as it already does after its own drop-triggered re-render, via a new `refresh:applied` custom event. Gate 2.11 (removed people no longer assignable, but still marked on their old tasks) turned out to already be fully correct from P2-01/P2-04's own work — this task's only change there was proving it with a dedicated E2E test. 2 new Go unit tests for the version counter (bumps on every write, unchanged by a rejected one) plus 1 new HTTP integration test. 4 new E2E tests: gate 2.10's three flows (a change appears in a second browser context within 60 seconds — `page.clock`, installed before that page loads, fast-forwards the exact `setInterval` refresh.js schedules, while real fetches inside it still complete over genuine network I/O; appears immediately on a dispatched `focus`; held back and offered via the notice while `data-refresh-busy` is set, directly, rather than choreographing a real mid-drag pause) and gate 2.11's already-working behaviour end to end (assignable-people list excludes a person deactivated after being assigned; their avatar and the details page's people select still mark and select them, "(removed)"). D-46 recorded (PLAN.md's own "D-15" placeholder for this task turned out to already be a different Phase 1 decision — a planning-time numbering slip, not touched). Full `npm run check` plus `test:screens` green locally. |
| P2-08 | Team view: lanes and workload | done | 216805a | https://github.com/stas-comp/comphq/actions/runs/35300314958 | New `internal/tasks/lanes.go`: `BuildLanes(tasks, people) []Lane` is a pure function (no DB access, reused as-is by My jobs in P2-10) grouping an already-filtered task list into an Unassigned lane (tasks with no assignees) followed by one lane per active person — sorted alphabetically by `BuildLanes` itself, not trusted from the caller, since a caller passing an arbitrary subset/order (My jobs will) shouldn't be able to violate gate 2.24's ordering. Each lane splits into Working on now (`doing`, unnumbered) and numbered Up next (`todo`); a task with more than one assignee appears in, and counts toward the workload of, every one of their lanes (gate 2.30). Workload blocks are grouped per task (`WorkloadGroup.Blocks` — a slice whose only meaningful property is its length, S=1/M=2/L=4 — the template ranges over it purely to repeat one block that many times, matching this codebase's "no template funcs, precompute view fields in Go" convention) and rendered as SPEC B4's exact worked example, "1 large · 1 medium · 1 small", largest first, a size omitted entirely once its count is zero. Unassigned deliberately gets no workload row — gate 2.27 says "each **person's** column," and Unassigned isn't one. New `Store.ListForTeamView` (non-removed, `todo`/`doing` only — Ideas and Done never appear, SPEC B4) feeds `GET /tasks/team`, min-lane-width 232px with the lanes container scrolling sideways on its own (gate 2.31), in a new `web/static/tasks/team.css`. New shared `{{template "tasks-view-switch"}}` partial (mirroring the existing `settings-subnav` pattern exactly) renders **My jobs · Board · Team**, added to the Board page now (My jobs itself, and its own use of `BuildLanes`, is P2-10 — its link already points at `/tasks`, which still redirects to the Board until then). 7 new Go unit tests (lane order defends its own sort even given people out of order; Unassigned excludes anyone assigned; grouping/numbering/sharing across lanes; workload block counts and the exact line text; one L equals four S in raw block count; Unassigned has no workload; `ListForTeamView` excludes Ideas/Done). 2 new E2E tests (lane order/grouping/numbering/blocks/text across an Unassigned task, a two-person shared task, and mixed sizes; standard page/axe/no-side-scroll checks). Full `npm run check` plus `test:screens` green locally. |
| P2-09 | Team view: assigning and reordering | done | 02d9418, d13efa9 | https://github.com/stas-comp/comphq/actions/runs/35321087919 | New `Store.Assign(taskID, fromPersonID, toPersonID, ...)` (SPEC B4/gate 2.28): either id can be 0 ("not given"), so the one method covers Unassigned→person, person→person (removing `from`, adding `to`, leaving every other assignee untouched), and — reusing it as-is — My jobs' future "Give back" (`to_person` empty). Assigning to someone already on the job is a harmless no-op, not a duplicate-key failure or a redundant activity row. `POST /tasks/{id}/assign` never changes stage or position. Reordering a lane's Up next (gate 2.29) needed no new endpoint at all — it reuses the existing move endpoint with a lane-scoped neighbour id (`LaneTask.PrevID`/`NextID`, computed the same way the Board's own cardView already does, just from a lane's slice of the shared order instead of a whole stage) — but that surfaced a real, untested gap: the move endpoint always redirected to `/tasks/board`, so a plain (non-JS) Move up/down click from the Team view silently bounced the person back to a page they never asked for. Fixed with an explicit `redirect_to=team` hidden field, an allowlisted value the handler checks rather than trusting the `Referer` header (D-47). New `web/static/tasks/team.js`: cross-lane drags (any list to any other lane, via one shared SortableJS group) call assign; a same-lane drag within one lane's own Up next list calls move; both re-render the whole lanes container from the server's response afterward, the same "server is the truth" pattern board.js already uses. Manually exercising the drag/assign/reorder flow in a browser (not a test) caught a second gap: an empty lane had no `<ul>` in the DOM at all (the list markup was wrapped in `{{if .WorkingOnNow}}`/`{{if .UpNext}}`), leaving nothing to drop onto — fixed by always rendering both lists per lane (matching the Board's own always-rendered-column pattern) with a small `min-height` so an empty one is still a real target. Running the full suite (not just this task's own new tests) against a shared, accumulating server surfaced two more real bugs standard for this codebase's own test-isolation lessons (D-41): `.team-lanes` becoming genuinely scrollable once enough lanes piled up tripped a real axe violation (`scrollable-region-focusable` — a scrollable region needs to be keyboard-reachable), fixed with `tabindex="0" role="region"`; and the cross-lane drag test itself needed converting to the `@fresh` isolated-server fixture, since a target lane could end up scrolled off-screen on a long-running shared worker. 7 new Go unit tests for Assign (each `from`/`to` combination, the third-assignee-kept case, no stage/position change, the already-assigned no-op, both invalid-input and missing-task errors) plus 1 new HTTP integration test. 4 new E2E tests (drag Unassigned→person and person→person keeping a third; the Assign to… button equivalent; reordering changes only the moved task's position in the Board's full shared order — proven by computing the exact expected reordering, not a naive index swap; 10 people at 1024×700 scrolls the lanes but not the page). First push's CI caught one more real find: at enough accumulated lanes, Chromium's `documentElement.scrollWidth` reported ~29800px on a 1024px viewport even though every individual lane measured correctly and `document.body.scrollWidth` stayed exactly right — a measurement quirk tied to the Team view being the first page with a genuine internal `overflow-x: auto` region, not a real side-scroll. Fixed by switching both the shared `expectNoSideScroll` helper and this task's own gate 2.31 test to measure `document.body` instead, plus a defensive `min-width: 0` on `main` in frame.css (D-48). Full `npm run check` plus `test:screens` green locally. |
| P2-10 | My jobs: the Tasks home, and Take it | done | d781100 | https://github.com/stas-comp/comphq/actions/runs/35324009328 | SPEC gates 2.32/2.33: `GET /tasks` (previously a redirect to the Board) is now My jobs — the person's own lane on the left, built by the exact same `LaneForPerson` wrapper around `BuildLanes` the Team view uses (SPEC B4: "built by the same code as their Team lane"), and a new `Store.ListUpForGrabs` on the right (unassigned `doing`/`todo` by stage then position, then unassigned `idea` by position — Done and anyone-else's-only jobs never appear). SPEC gates 2.34/2.35: new `internal/tasks/take.go`, `Store.Take(taskID, beforeID, afterID, actorID, now)` — in one transaction, refuses (a sentinel `ErrTaskAlreadyTaken`) if anyone is already on the job, otherwise adds the current person; an idea becomes a todo, placed at the drop position or the bottom; a todo/doing job keeps its own position unless a drop position is given; records `assigned` activity always, plus `moved` when the stage changed. `POST /tasks/{id}/take` (optional `before_id`/`after_id`) exposes it — SPEC B4's exact 409-with-names conflict response is explicitly P2-11's own scope (its touches list says so directly), so for now an already-taken job just falls back to re-showing the current page, the same "changed under us" pattern the move endpoint already uses for a vanished neighbour. New `web/templates/tasks/myjobs.html`/`web/static/tasks/myjobs.css`/`web/static/tasks/myjobs.js`: the Up next list and the Up for grabs list share one SortableJS group so dragging a card across takes it at the drop position, mirroring `team.js`'s own cross-list pattern exactly (including always rendering both lists even when empty, per P2-09's own empty-drop-target lesson); a same-list drag within Up next (no take endpoint wired for plain reordering in this task's scope) is deliberately left a no-op, restored by the same post-drop server refresh team.js already relies on for its own undefined same-lane drops. Manually exercising the finished page in a browser (not a test) confirmed no CSP/console errors and the Take it button's idea→todo transition end to end before writing the E2E suite. 8 new Go unit tests (`ListUpForGrabs` ordering/exclusions, `LaneForPerson` matching the Team lane, every Take combination — position kept, drop position honoured, idea→todo at the bottom and at a drop position, activity rows for both changes, the already-taken refusal changing nothing, the not-found case) plus 3 new HTTP integration tests (My jobs' own/shared/other/idea/todo/doing/done filtering, Take keeping position, idea→todo over HTTP) — replacing the now-obsolete `TestTasksRedirectsToBoard` and extending the DELETE route-table test to the new endpoint. 6 new E2E tests (a fresh name lands on My jobs; the fixture matrix from gate 2.33 asserting exactly what shows on each side, in the right Up-for-grabs order; take by drag placing the job where it was dropped, using the same poll-for-`sortable-chosen`-then-retry technique D-41/P2-09 already established, on an isolated `@fresh` server since Up for grabs otherwise accumulates every unassigned task any other test on the shared worker has ever created; take by button keeping the shared priority order unchanged; an idea taken moving to To do; standard page checks). Full `npm run check` plus `test:screens` green locally, including a `--workers=1` full-suite run (130 tests) to rule out the kind of accumulation-dependent flake P2-09 hit. |
| P2-11 | My jobs: take conflicts, moving jobs on, Give back | done | 65b2f65 | https://github.com/stas-comp/comphq/actions/runs/35326564983 | SPEC gate 2.36: `Store.Take`'s already-taken guard now returns a new `*TakenError{Names}` instead of the P2-10 placeholder sentinel — fetched inside the same transaction (a new `assigneeNamesTx` helper) so the names are exactly who won the race, before rolling back and changing nothing. `POST /tasks/{id}/take`'s handler renders My jobs directly with status 409 and the exact "*Name* has just taken this job." message (a new shared `Message` field on `myJobsPageData`, the same `{{if .Message}}` convention `renderBoard` already uses) rather than redirecting — a genuine HTML response at any status still renders normally for a plain form POST, so the button path needed no JS at all; the drag path in `myjobs.js` checks for a 409 and swaps the response body straight in as the new `.myjobs-page` (widened from P2-10's own `.myjobs-columns` specifically so the message paragraph is inside the swapped fragment too) instead of re-fetching. SPEC gate 2.37: Start/Done reuse the existing move endpoint (`to_bottom` into `doing`/`done`, exactly like the Board's own "Move to…") and Give back reuses the existing assign endpoint (`from_person` = me, no `to_person`) — both already supported everything needed with zero Store changes; the only real gap was the move/assign endpoints' redirect always landing on Team, so `redirect_to` (D-47's own pattern from P2-09) is generalized from a single Team special-case into a small shared `redirectTargetFrom(r)` helper reused by both handlers, now also accepting `myjobs`. Manually exercising Start/Done/Give back in a browser (not a test) confirmed each transition and no CSP/console errors; verifying the actual two-person race needed Playwright's real separate browser contexts instead, since this app's own single-profile browser tool shares one cookie jar across tabs. Extended the existing Go unit test for the conflict path to assert the returned names via `errors.As`, and added 2 new HTTP integration tests (the 409 response and message over real requests changing nothing; Start/Done/Give-back's stage and assignee bookkeeping, including Give back leaving a still-shared job off Up for grabs). 6 new E2E tests in `e2e/tasks/myjobs-conflict.spec.ts` (two real browser contexts racing for the same job — the first's button click succeeds, the second's stale button click gets the exact message and adds nobody; the taken job leaving the other context's Up for grabs within 60 seconds and immediately on focus, reusing refresh.spec.ts's own `page.clock` technique; Done leaving My jobs and — via the existing `/__test/tasks/set-done-at` backdating route from P2-06 — reaching Finished tasks the same way any other Done task does after the real 14-day rule; Give back returning a solo job to Up for grabs but leaving a still-shared one off it; standard page/axe checks with the conflict message actually showing). Full `npm run check` plus `test:screens` green locally, including a `--workers=1` full-suite run (136 tests). |
| P2-12 | Seeder and speed for Board, Team and My jobs | done | 2572003, 50c4492 | https://github.com/stas-comp/comphq/actions/runs/35329903827 (go/browser/speed, all green) | `e2e/seed/main.go` extended (SPEC's "test library": 2,000 tasks, 10 people): people are `Person 01..NN`; tasks vary size/stage/due-date/notes with the same office vocabulary as article titles; assignment is unassigned (1/7, for Unassigned/Up for grabs), shared between two people (1/15, gate 2.30/2.33), or solo, all deterministic by index so re-seeding is byte-for-byte reproducible. `e2e/speed/tasks.speed.spec.ts` (Board, Team, My jobs, a task page, Finished, Removed each median-of-5 ≤ 1,500ms; Team and My jobs also re-run through axe + `expectNoSideScroll` with the real seeded library, gates 2.31/2.38). First run failed gate 2.21 outright — 1,961ms against the 1,500ms Board budget — because the seeder's first cut put ~95% of the 2,000 tasks into live, unremoved stages, rendering ~1,850 cards (~43,000 DOM nodes) on one Board page. Profiling (raw curl: ~60ms; `domContentLoadedEventEnd` within 2ms of `domInteractive`, ruling out SortableJS init; stripping the "Move to…" control alone in isolation cut parse+layout by 58%) placed the cost squarely in the browser parsing/laying out that much real, required markup — not a missing index, and not something to fix by lightening or JS-ifying an already-tested, working control (gates 2.04/2.05) just to chase a number. Fixed at the source instead: the seeder now spreads the 2,000 total as 75% removed, 10% aged into Finished tasks (gate 2.08's 14-day rule), 15% genuinely active — matching how a real team's board actually looks after 2,000 cumulative tasks (a modest current backlog plus a long tail of finished/cancelled work), not an unusable, never-real 1,850-live-card page (D-50). Every Tasks page now loads in well under 200ms with the full library, including the resulting 1,500-row Removed tasks list — no pagination needed anywhere (PLAN's own risk table reserves that remedy for Finished/Removed specifically). Full `npm run check` plus the `[speed]` project green locally (including the pre-existing KB speed tests, unaffected), plus a `--workers=1` full `e2e` run (136 tests) to rule out accumulation-dependent flakes. |
| P2-13 | Recurrence engine | done | a6840c1 | https://github.com/stas-comp/comphq/actions/runs/35342099195 | New `internal/calendar/recur` package: `Occurrences(event, exceptions, from, to) []Occurrence`, a pure function (no DB access) shared by the Calendar and the Briefing, matching SPEC B3's `events`/`event_exceptions` field names directly (`Recurrence` "none"/"weekly"/"monthly"/"yearly", `Kind` "moved"/"cancelled") so P2-14's store layer can map straight onto it. Weekly steps every 7 days from `start_date`; monthly and yearly walk calendar months/years (via `time.Time.AddDate`, which normalises month/year overflow itself) reclamping the day fresh each step rather than carrying a clamped value forward (SPEC B4: 31 Jan → 28 Feb → 31 Mar, not stuck at 28); yearly's only clamp is 29 Feb in a non-leap year. Multi-day length (`end_date − start_date`) is preserved on every generated occurrence. Exceptions are applied by two passes: skip a candidate's original date entirely if it has any exception (cancelled or moved), then separately emit every `moved` exception whose *new* dates overlap `[from, to]` regardless of whether its *original* date does — the only way "moved outside the window still appears when its new date is inside" works without generating occurrences over an unbounded range. Title and notes always come from the event, never the exception, matching SPEC B4's "Change just this one only offers date and time changes" constraint even in the pure function. 10 new unit tests covering the exact gate-2.15 dates plus every case PLAN.md's own table lists (weekly, monthly-clamped, yearly-clamped, `until` boundary, multi-day across a month end, moved, cancelled, moved-from-outside-the-window, a multi-year range) plus one confirming a moved occurrence still carries the series' own title/notes. Full `npm run check` (Go suite plus the full Playwright `e2e`/`a11y`/`screens` projects, unaffected by this pure-logic-only change) green locally. |
| P2-14 | Events: add and edit, month view, list view | done | 26220a5 | https://github.com/stas-comp/comphq/actions/runs/35344983969 | Calendar replaces the "Coming soon" placeholder: new `internal/calendar` package (store, handlers, section registration) and `migrations/calendar/0001_events.sql` (`events`/`event_exceptions` per SPEC B3, matching `internal/calendar/recur`'s own field names exactly so P2-13's pure function wires straight on). `Store.Occurrences(ctx, from, to)` loads every non-removed event, expands each through `recur.Occurrences` with its (currently always-empty — nothing writes to `event_exceptions` until P2-15) own exceptions, and merges/sorts the result — one query serving both the month grid and the 12-week list. Routes exactly per PLAN: `GET /calendar` (month, `?month=YYYY-MM`, defaulting to the current month), `GET /calendar/list`, `GET /calendar/new?date=` (prefilled from the clicked day), `POST /calendar/events`, `GET /calendar/events/{id}` (details, with an editable form built in — Tasks' own details-page pattern, so there's no separate "edit" route) and `POST /calendar/events/{id}`. SPEC gate 2.13's "Show in briefing ahead" (not early, or N days/weeks) is one `notice_days` column, split into an amount+unit pair for the form and recombined on save (weeks stored as days x7, redisplayed in weeks when evenly divisible, per SPEC B3). Gate 2.20's "Last changed by *name* · *date time*" reuses the existing `internal/app/format` helpers already established by the Knowledge Base. A real, non-obvious bug found by the new integration tests (not a design flaw, a wiring gap): `internal/app.NewServer`'s `template.ParseFS` call lists each section's template glob by hand, and Calendar's own directory was missing from it — every page 500'd despite `go build` succeeding, since the embed and the parse are two separate steps (D-51). New `internal/app/format` reuse, `web/static/calendar/calendar.css`, and four templates sharing one `calendar-event-form` partial between the Add and Details pages. 11 new Go unit tests for the store (every field round-tripping, defaulting, all three validation rules, `Update`'s own last-changed bookkeeping, `Occurrences` wiring and its removed-event exclusion) and 8 new HTTP integration tests (create/validate/redisplay, edit, the month view showing a created event's chip on the right day, and the route-table DELETE check). 7 new E2E tests (`COMPHQ_TEST_TODAY=2026-09-16` for gate 2.12: every visible Saturday actually is one, today is marked, Previous/Next/Today navigate, clicking a day prefills the new-event date; the 12-week list in date order excluding a further-out event; gate 2.13's full field round-trip; gate 2.20's last-changed line updating after an edit; adding an event with keyboard only; standard page/axe/no-side-scroll checks for all four pages). One planned E2E test (a blank-title refusal message) turned out to be unobservable through a real browser — the title input's own `required` attribute blocks the browser from ever submitting it, matching the Board's own precedent of leaving that check to its HTTP integration test alone. Removed `/calendar` from gate 1.08's "Coming soon" list (PLAN's own rule 2.5). Full `npm run check` (Go suite, full Playwright `e2e`/`a11y`/`screens`) green locally, including a `--workers=1` full run. |
| P2-15 | Repeating events: just this one, change all, cancel, remove | done | 1b6de96 | https://github.com/stas-comp/comphq/actions/runs/35348414475 (green on rerun — the first attempt hit the pre-existing, already-documented gate 1.29 KB search flake from P1-36/P2-04, unrelated to this task) | SPEC gate 2.15's own choice ("Change just this one" / "Change all") is driven entirely by data already on hand: `recur.Occurrence` gained an `OriginalDate` field (equal to its own date, except for an already-moved occurrence, where it's the exception's key) so a chip's link can always carry `?occurrence=<original date>`; `handleEventDetails` shows the choice only when that param is present on a repeating event, and falls straight through to the ordinary edit-everything page otherwise — a non-repeating event, or a plain visit, never sees it. "Change just this one" is a new small store/handler/template trio scoped to gate 2.15's own constraint ("only offers date and time changes plus cancel" — never title/notes, which SPEC B4 says always come from the series): `Store.SetMovedException`/`SetCancelledException` upsert one `event_exceptions` row keyed by `(event_id, original_date)` — a real SQLite `ON CONFLICT ... DO UPDATE`, since re-editing an already-moved occurrence is expected, not an error. `Store.OccurrenceForEdit` pre-fills that form from an existing exception if one exists, otherwise from the series' own computed dates/times (reusing `recur.LengthDays`, now exported, so a multi-day series' length survives into the pre-filled end date rather than silently collapsing to one day). Remove/Restore/`GET /calendar/removed` mirror Tasks' own precedent exactly (soft-delete via `removed_at`, nothing ever physically deleted). 12 new Go unit tests (the exact gate 2.15 dates at the store layer, re-editing a move, gate 2.16's cancel leaving the rest untouched, every validation rule, the pre-fill logic both with and without an existing exception, Remove/Restore/ListRemoved and their not-found/not-removed error cases) plus 5 new HTTP integration tests (the choice appearing only for a repeating event reached via `?occurrence=`, the exact 2.15 script, gate 2.16, and the full Remove → Removed → Restore round trip) — and the route-table test extended to the two new URL shapes. 6 new E2E tests covering gates 2.15–2.18 exactly as PLAN.md's own script describes, plus standard page checks for Removed events. A real, `--workers=1`-only test bug found and fixed along the way (not a product bug): an "occurrence moved away" assertion checked a day cell's *total* chip count, which broke once an earlier, unrelated test's own leftover event legitimately shared the same literal date on the shared worker — fixed by scoping every such assertion to the specific event's title, the same way every "chip is here" assertion already had to be (D-52). Full `npm run check` (Go suite, full Playwright `e2e`/`a11y`/`screens`) green locally, including two `--workers=1` full runs to confirm the fix actually held. |
| P2-16 | Task deadlines on the calendar | done | 9f6e176 | https://github.com/stas-comp/comphq/actions/runs/35351562239 | New `internal/tasks.DueTasks(ctx, from, to) []DueTask` (unfinished, unremoved, due date in range) is the only new surface on the Tasks side. `internal/calendar` never imports `internal/tasks` at all (SPEC B2: "sections never import each other's internals") — it defines its own `DueTask` shape and a `DueTasksSource` interface, injected into `Section(srv, dueTasks)` at registration; `cmd/comphq/main.go` is the one place allowed to know about both sections, via a small `taskDeadlines` adapter converting `tasks.DueTask` → `calendar.DueTask`. The month view's old event-only `occurrenceView`/`.Occurrences` became a unified `chipView`/`.Chips` (`Kind` "event" or "task") so a day cell renders both kinds through one loop — solid accent-coloured for an event, outlined with a CSS-drawn tick-box square for a task deadline (SPEC A7: "so the two are never confused"), `data-kind` on both for tests to key off. A real bug surfaced immediately by the integration tests (not a design flaw): the `calendar-month.html` template still referenced the old `.Occurrences`/`.DetailsHref` field names after the rename, 500ing every month-view request until fixed — caught before it ever reached a manual browser check. 3 new Go unit tests for `DueTasks` (excludes done/removed, respects the date window, orders by due date) plus 2 new HTTP integration tests using a `fakeDueTasksSource` (PLAN's own explicit test guidance — Calendar's tests never touch a real Tasks store), one confirming the task chip's `data-kind`/href and one confirming the pre-existing event chip still carries `data-kind="event"`. 1 new E2E test: a due task's outlined chip opens the task page; a task marked Done and a removed task both stay off the calendar entirely. Manually verified in a browser first: an outlined checkbox chip next to a solid orange event chip on the same month grid, no console/CSP errors. Full `npm run check` (Go suite, full Playwright `e2e`/`a11y`/`screens`) green locally, including a `--workers=1` full run. |
| P2-17 | Calendar speed, and Tasks/Calendar accessibility and size sweep | done | 19dbbb3 | https://github.com/stas-comp/comphq/actions/runs/35370637923 (go/browser/speed) | `e2e/seed/main.go` now also seeds 300 events (`seedEvents`, `-events` flag): 100 repeating (70 yearly, 20 monthly, 10 weekly — skewed to yearly because that is this app's real use per SPEC O2.2, and because a weekly event fans out to a dozen List rows apiece), about one in seven removed so Removed events has content, 20 moved/cancelled exceptions, dates spread from a year back to two years ahead, a mix of all-day/timed/multi-day. Unlike the Board (D-50), the Calendar needed no reshaping of the library: the month and list views are windowed by date by construction, so any one screen only shows the occurrences falling in it — the month and list timing test (5 navigations each) took ~1.2s in total. New `e2e/speed/calendar.speed.spec.ts` (month and list median of 5 ≤ 1,500 ms; axe plus the window-size check on every Calendar page type — month, list, add form, an event's details, Removed events — against the seeded library) and `tasks.speed.spec.ts` gained the same axe/size check for the Tasks pages P2-12 hadn't covered with the library (Board, a task page, Finished, Removed), completing gate 2.23 with and without seeded data (the without-data half already lives in each page's own spec). That last test needed `test.setTimeout(120_000)`: axe itself takes ~45s walking a 1,500-row Removed list — the check is unchanged, only its time budget. Full local `e2e`/`a11y`/`screens`, Go suites and the `[speed]` project all green. |
| P2-18 | Export: tasks and events spreadsheets | done | a496cb7 | https://github.com/stas-comp/comphq/actions/runs/35371977707 | SPEC gate 2.22: Export everything's zip now also holds `tasks.csv` and `events.csv`. New `Store.ExportRows(ctx) [][]string` in both `internal/tasks/export.go` (Title, Column, Size, People, Due date, Created by, Created, Finished, Removed — Column and Size as the words the app shows, People joined with commas) and `internal/calendar/export.go` (Title, Date, End date, Start time, End time, Repeats, Until, Show ahead, Notes, Removed — "Show ahead" worded like the form: "Not early", "3 days", "6 weeks"); both include removed rows with their Removed date. Settings gains a one-method `CSVSource` interface, injected via `Section(srv, tasksCSV, eventsCSV)` — the stores satisfy it directly because it deals only in `[][]string`, so unlike P2-16 no adapter is needed; `cmd/comphq/main.go` passes `&tasks.Store{...}` and `&calendar.Store{...}`. `writeCSV` emits a UTF-8 BOM and CRLF (D-54, which also records the decision PLAN left open: readable dates only, no extra ISO columns — `comphq.db` in the same zip already has those). Tests: a Go unit test per `ExportRows` (exact rows including a shared task, a removed one, multi-day/yearly/until event) plus `showAheadLabel`; the settings integration test now uses fake `CSVSource`s and asserts both files exist with the BOM and CRLF bytes; a new E2E test creates a real task and event, downloads the export, unzips it, and asserts BOM, exact headers and a known row with readable dates, while the existing gate 1.34 offline-export test still passes unchanged. Full local Go suites and `e2e`/`a11y`/`screens` green. |
| P2-19 | README for Phase 2 and full Phase 2 sweep | done | 9c8655f | https://github.com/stas-comp/comphq/actions/runs/35372772146 (full dispatch: go, browser, container incl. the offline Phase 2 E2E run, speed, screens — all green) | `README.md` gains four plain-language sections — **Using Tasks** (My jobs, Take it and taking by drag, Start/Done/Give back, the Board, Finished and Removed tasks, the Team view), **Using the Calendar** (month and list, adding events, repeats and the 31st / 29 February rules, "Change just this one" versus "Change all", Removed events), **Entering your yearly events** and **Moving your Trello cards** (the O2.2 owner check, click by click) — plus a short note that Export everything now includes the two spreadsheets, an updated hands-on table row, and the README heading test (`tools/policy`, gate 1.44) now requires the four new headings. Every remaining Phase 2 page type joined `e2e/helpers/page-registry.ts` (Board, Team, Finished, Removed, Calendar list, new event, Removed events), so the shared axe, screenshot and sideways-scroll checks cover them too (the pages needing an id — a task, an event — are covered by their own specs and the seeded-library speed tests). New `reports/gates-phase-2.md`: one row per gate 2.01–2.38 with SPEC's description, the real test file(s) and test names, all 38 marked Pass from the single full dispatch, plus a note of the Phase 1 gates whose tests changed (1.08, and 1.07/1.09/1.10's wider page coverage). The full dispatch also re-ran every Phase 1 gate. |
| P2-20 | Release v0.2.0 and Phase 2 report (STOP S4) | done | 9175505 (tag v0.2.0) | https://github.com/stas-comp/comphq/actions/runs/35376109533 (release.yml incl. publish; full dispatch 35374630547) | Release v0.2.0 published, anonymous ghcr manifest 200. `reports/phase-2-report.md` and `reports/phase-2/screens/` written. Owner checks O2.1, O2.2 (and O1.3) recorded as pending by explicit owner instruction (2026-09-18); see D-55. |
| P3-01 | Briefing rules as a pure function | done | 7a9bd58 | https://github.com/stas-comp/comphq/actions/runs/35388569536 | `internal/briefing.Build` (pure; SPEC A8 rules for Must be done, This week, Coming up, stamps, YOURS, "until"); `tasks.Store.BriefingTasks`; `calendar.Store.UpcomingWithNotice` (+ `loadSeries` refactor shared with `Occurrences`). Unit tests use the exact SPEC dates and run in three time zones. |
| P3-02 | Briefing page | done | 006d2d8 | https://github.com/stas-comp/comphq/actions/runs/35389518131 | Briefing renders at `/` and `/briefing` (headline, three sections, stamps, YOURS, Just mine, friendly empty messages, links). New: `internal/briefing` section + adapters in `cmd/comphq/briefing_sources.go`, `web/templates/briefing/`, `web/static/briefing/`, `NavItem.AlsoCurrentAt`. E2E `e2e/briefing/briefing.spec.ts` covers gates 3.01–3.11 (+ axe/size for a populated page) with three test dates. Rule 2.5 changes: gate 1.08 test replaced; picker/skeleton tests now expect the Briefing at "/"; `ComingSoon` removed. See D-56. |
| P3-03 | Briefing refresh, speed and checks | done | 5c28485 | https://github.com/stas-comp/comphq/actions/runs/35390099536 (go/browser/speed, all green) | `GET /briefing/version` (tasks_version + calendar_version); Calendar gains `calendar_version`, bumped in the same transaction as every write (`Store.inTx`). E2E `e2e/briefing/refresh.spec.ts` (two contexts: `page.clock.runFor(61_000)` and focus, for a task, an event edit and a new event). `e2e/speed/briefing.speed.spec.ts` (median of 5 <= 1,500 ms with the seeded library, plus axe and size). See D-57. |
| P3-04 | Final README and full sweep | done | 8764b64 | https://github.com/stas-comp/comphq/actions/runs/35391128930 (full dispatch: go, browser, container, speed, screens, all green) | README gains "Using the Saturday Briefing" (Just mine, show ahead explained) and the Phase 3 hands-on row; the README policy test requires the new heading. `reports/gates-phase-3.md` (13 rows). New populated-Briefing screenshot spec. README sections re-read against the current app: the Briefing is now the first screen, no placeholder wording remains. |
| P3-05 | Release v1.0.0 and final report (STOP S5) | blocked | 5fff7d3 (tag v1.0.0) | https://github.com/stas-comp/comphq/actions/runs/35395930805 (release.yml incl. `publish`); full dispatch https://github.com/stas-comp/comphq/actions/runs/35394576555 (go, browser, container, speed, screens) | v1.0.0 published, anonymous ghcr manifest 200. `reports/phase-3-report.md`, `reports/gates-phase-3.md` and `reports/phase-3/screens/` written. Waiting for owner checks O3.1, O3.2 (and the still-pending O1.1-O1.4, O2.1-O2.2). Two flaky search tests fixed at their cause on the way (D-58). |
| P4-01 | Assigned ideas appear in My jobs and Team | done | 5080773 | https://github.com/stas-comp/comphq/actions/runs/35463455958 | Written failing-first (3 unit tests failed against today's code, then passed). `ListForTeamView` now selects ideas; `Lane.Ideas` is its own group (My jobs **Ideas I'm on**, Team **Ideas**); workload counts todo/doing only; unassigned ideas stay in Up for grabs and out of the Unassigned lane. **Test corrected:** `TestListForTeamViewExcludesIdeasAndDone` pinned the old behaviour gate 4.49 overturns, now `...IncludesIdeasButNotDone` — list under "Tests corrected" in the Phase 4 report. Gate 4.51 read as the three unfinished columns: a task created straight into Done stays on the Board only (D-59). E2E `tasks/create-assigned.spec.ts` uses the board's current add form through one helper; P4-06/P4-08 must repoint it. Styling of the new groups is P4-10. |
| P4-02 | Fonts, tokens, and the mockup-comparison harness | done | 3033519 | https://github.com/stas-comp/comphq/actions/runs/35464433394 | theme.css now holds SPEC B9.2's tokens exactly plus Big Shoulders Display, IBM Plex Mono (both vendored, OFL, Latin subsets, checksummed) and Atkinson; Archivo deleted. `e2e/design/mockup.ts` is the harness: `Mockup.open` (mockup in a second tab, every network request refused, reduced motion so no read is mid-fade), `expectMatchesMockup(mockup, page, {mockup, screen, app}, props)`, first-family font comparison, shorthand expansion; 7 self-tests + 7 foundation tests. Policy tests (`tools/policy/design_test.go`): token table, no colour/font literals in section CSS, no white-on-accent / accent-as-text, no Archivo. Fixed the five white-on-accent pairs. Moved the 12 avatar colours into theme.css and repointed `avatar_test.go` to it (same assertions). Headings: h1/h2 display 900 uppercase, h3 body bold (mockup's h3 is a card title). D-60; D-09 marked superseded. Every later design task should use `test` from `e2e/design/fixtures.ts` (gives `mockup`). |
| P4-03 | Shared parts and styled controls | done | a424c3b | https://github.com/stas-comp/comphq/actions/runs/35466278868 | Shared parts in theme.css from the mockup's own values, proven by 34 mockup comparisons on a test-mode `/__test/parts` page (buttons, fields, seg, stamps, sizes, circles, workload, card, panel head, date) + axe + no-side-scroll. Every control in every template has a theme class (policy test walks all templates). Date field is a day-first text box checked server-side (D-61; stores now refuse non-real dates). Avatars: mockup's 4 colours + accent circle for the current person. Partials: size, people, workload (with spoken label, unit-tested); stamp and seg shared by CSS class only. Removed section-CSS rules that restyled controls. **Tests corrected (list in Phase 4 report):** calendar integration test + calendar/details e2e date assertions now day-first; calendar keyboard-only test types 01/10/2026; board drag helper scrolls cards into view; avatar tests rewritten for the new palette; `.task-avatar`→`.av` selectors. Board card due date still shows ISO — P4-06 formats it. |
| P4-04 | Icons and the icon-only button rule | done | c00afc6 | https://github.com/stas-comp/comphq/actions/runs/35466986483 | 15 icons in `web/static/theme/icons/` (9 new, 5 section icons redrawn from the mockup, + import-word for P4-12), each `viewBox 0 0 24 24`, 2px round stroke, `currentColor`; drawn by a CSS mask (`.icon .icon-<name>`) so they take the holder's colour; nav now uses them (`NavItem.Icon` is a name, not a path). `.icon-btn` = the mockup's 24px mover box. Four icon-only buttons only, via `app.NewIconButton(kind, taskTitle, disabled)` (panics on a fifth kind) + `part-icon-button` partial; not yet used on real screens (P4-06/07/10 use them). Policy: icon files' attributes and classes, and no button without words outside the partial, title==aria-label. 6 e2e (files served, paint, colour inheritance, mockup sizes, names) + axe. |
| P4-05 | The frame | done | c2c1448 | https://github.com/stas-comp/comphq/actions/runs/35467589539 | Sidebar (224px, stacked COMP/HQ wordmark, links per mockup, current = accent block + ink text, Settings pinned to the foot, mono version) and top bar (styled `.search` with icon + `/` hint that is a real shortcut in search.js, "You: name" + accent circle + Change) — 10 mockup-comparison tests incl. hover and focus states. `format.Initials` shared by the frame and tasks. `main` now full width with the mockup's padding (was capped at 60rem). One policy exception recorded in D-60: accent text is allowed for `.wordmark strong` only (accent on navy, gate 4.13). The parts-page `.field` search test from P4-03 still passes. |
| P4-06 | Board: controls, columns and cards | done | b7e8ac8 | https://github.com/stas-comp/comphq/actions/runs/35468603849 | Board as drawn: `.tools` row (h1, seg switch, one Everyone/My tasks/person drop-down, word filter, primary + Add task at the right), 4-column grid, tinted To do column, headings with counts + hint, cards (rank on To do only, title, short date, people, size chip, icon move buttons via `part-icon-button`), struck-through grey done cards, Finished/Removed links under Done. Add-task form removed from the Board; new `GET /tasks/new` page (same field ids + notes/size) is + Add task's target and the no-JS path. 9 mockup/behaviour tests (`e2e/design/board.spec.ts`). **~110 test lines that add a task now call `openNewTask(page)` first (helper `e2e/helpers/tasks.ts`); tests corrected (list in D-61b): gate 2.01 hint, gate 2.03 short date, Move up/down by accessible name, filter test drop-down, integration due-date assertion.** Move to… kept (worded) as the drag's button equivalent. |
| P4-07 | Card actions: remove, give back, assign | done | eebfae7 | https://github.com/stas-comp/comphq/actions/runs/35471441199 | Remove (trash icon), give back (icon, only for a person on the job; only that person comes off) and assign (people circles -> menu) on every Board card, over the existing endpoints; `redirect_to=board` added to the allowlist. The circles are a link to `/tasks/{id}/people` (no-script path: small page, a button per person); board.js fetches it as a fragment (`app.RenderPartial`) and shows a menu on the card (Escape / outside click closes, menu stays open between picks, page never reloads); remove/give-back also swap the board in place. 7 e2e incl. JS-off run, Activity rows, axe with menu open. **First push failed CI (Team page axe test >30s):** cause was test people piling up on CI's single-worker server; fixed by retiring each test's people when it ends (D-61c) — no limit raised, no retry. Team view's per-job assign select is unchanged (P4-10 looks again). |
| P4-08 | The task window: the dialog, and creating a task | done | bbceb7b | https://github.com/stas-comp/comphq/actions/runs/35472212095 | One native `<dialog id=task-window>` (`tasks-window` partial, `window.js`, window styles in theme.css from shared parts). + Add task opens it with the new-task form fetched as a fragment (`/tasks/new?fragment=1`, `app.RenderPartial`); save posts `fragment=1` -> 204 (close + board swapped in place) or 422 (form back with message and typed values). Closes with Escape / close icon / Cancel / backdrop (press must start on the backdrop); `confirm('Leave without saving?')` when dirty; focus returns to the opener; native dialog inertia keeps Tab off the board. No-JS: + Add task is still a link to `/tasks/new` (JS-off test + failed-fragment test). New fifth icon-only button `IconClose` (gate 4.25 asks for a close icon) — still title==aria-label, still only in the partial. 9 e2e. The window has no mockup picture: **tell the owner to look hardest at it (O4.3)**. Reading/editing states come in P4-09; the body div carries `data-state`. |
| P4-09 | The task window: reading, editing and history | done | 6a9d0f9 | https://github.com/stas-comp/comphq/actions/runs/35472924486 | Reading face (`tasks-window-read`, from `GET /tasks/{id}?fragment=1`): facts, description as text (no box, line breaks kept), History behind a closed `<details>`, primary Edit. Edit = `&mode=edit` = the task page's own form (`tasks-details-form`, one template, ids unchanged); save posts `fragment=1` to the existing `POST /tasks/{id}` -> 204 (back to reading, History refreshed, board swapped in place) or 422 (form + typed values). Window and page show identical History rows (tested). `/tasks/{id}` unchanged: direct, refresh and Calendar-link tests. JS-off test. 8 e2e + axe (reading, History open, editing). **Tests corrected:** every test that clicked a card title to reach the page now uses `openTaskPage` (goes to the link's address) since that click opens the window (gate 4.23); details keyboard-only test likewise. Details form label Notes -> Description. D-62 extended. |
| P4-10 | My jobs and Team | done | b0016dc | https://github.com/stas-comp/comphq/actions/runs/35473906357 | My jobs: `.area.mine` tinted, `.area.grabs` dashed/no fill, "Drop a job here to take it" is a real target inside Up next; Team: mockup `.team` grid of `.lane`s (Unassigned dashed/no fill + "N jobs waiting for someone", own lane tinted + YOU stamp, lane heads with circle/name/blocks/summary, own blocks deep accent). Shared `.sub`, `.also` ("Also on it: …"), `.acts`, `.drop`, `.legend`, `.how` in theme.css. Take it / Give back / Start / Done ✓ worded `.mini`; card titles open the window on both screens (window.js now refreshes whatever fragment the page marks). `Lane.Decorate`, `LaneTask.DueLabel/AlsoOn`, `JobCount`. Ideas I'm on / Team Ideas styled. Team's per-job Assign to… select kept (gate 2.28). 7 mockup tests. **Tests corrected:** Team numbering test (figure, not "1." text), Team Move up by accessible name, P4-03 circle test finds its own card. D-62b. |
| P4-11 | Calendar | done | b833b90 | https://github.com/stas-comp/comphq/actions/runs/35474644358 | Month grid per mockup (still a `<table>`, `border-collapse: separate`): Saturday heading solid accent + ink text, Saturday column accent wash, out-of-month a third shade (`--color-day-out`; three distinct values asserted), today's number in an ink chip, event chips ink / lighter navy on later days of a multi-day event (`chipView.Continues`), task chips white + ink outline + empty square, red when late (`chipView.Late`), legend under the grid, `.tools` row (month name is now the h1; Previous/Today/Next `.btn`, Month/List seg, primary + Add event). 7 mockup tests incl. `::before` of the task chip. **Departure from the mockup (D-63):** out-of-month day numbers keep muted text (5.2:1) instead of #A3A7B2 (2.2:1) because 4.03 needs 4.5:1. **Tests corrected:** month-navigation tests read the month from h1, not h2. |
| P4-12 | Knowledge Base | done | d6a14c1 | https://github.com/stas-comp/comphq/actions/runs/35475637112 | Article per mockup (`.kb-article`: 54px display title, h2 26px / h3 20px display, body 17px/1.65 readable face, ink-header tables), byline "Updated by name · date" from the latest history row + right-hand group History (N) / Archive / primary Edit; search panel (category small mono caps first, title, accent `mark`, selected tinted, footer line) + full results page as one bordered list; `.kb-highlighted` = mockup `.hit` + Clear highlights; editor: one bordered bar with separators, Import from Word primary + icon at the right end, display-face title over a white sheet, IMPORTED notice (`ComphqUI.showImported`, now after every import) and red dashed `.missing` boxes (token `--color-danger-ink`). 8 mockup tests (`e2e/design/kb.spec.ts`). **Found and fixed at its cause:** the mounted editor's textbox and its two hidden file inputs had no accessible name (the old page check ran before the editor mounted) — aria-labels added. D-63b. |
| P4-13 | Briefing and the name picker | done | 68bea1c | https://github.com/stas-comp/comphq/actions/runs/35476827050 | Briefing: h1 keeps the SPEC 3.02 text but is drawn as day (ink) + date (deep accent) at 76px with the mono tag above it (grid order); shared seg now reads Everyone / Just mine (4.06); panel heads with counts; `.item` cards (title, date line, people circles, stamps right — YOURS drawn left of the date stamp via row-reverse, note strip, overdue outlined `--color-danger-outline`). Avatar palette moved to `internal/people` (`people.AvatarClass`; `tasks.AvatarClass` delegates; contrast test moved) so the Briefing draws circles. Picker: full ink screen, stacked wordmark (styles now in theme.css), 4-column names grid, accent on hover. 5 mockup tests. **Tests corrected:** Briefing people read from the circle's title; 'Everything' -> 'Everyone'. D-63c. |
| P4-14 | Full sweep: checks, speed, screenshots, README | done | 53c594f | https://github.com/stas-comp/comphq/actions/runs/35477801423 (full dispatch: go, browser, speed, screens, container incl. offline run and upgrade/rollback — all green) | Speed with the 2,000-task/10-person library passes (locally too). Every Phase 1–3 test still runs at the same thresholds (corrections listed in decisions D-59…D-63c and to be summarised in the report). `reports/phase-4/screens/` (38 images, committed) = every page type + populated Board/My jobs/Team/Calendar/article/search/editor + the task window in each state (new, reading, History open, editing) + the people menu, all with the mockup's own example content (`e2e/screens/populated.spec.ts`, fixed today 19 Sep 2026). README: card icons, people menu, task window, day-first dates, Calendar/Briefing wording, Trello steps. A card's Move to… button equivalent now sits behind an expander (gate 2.05 test opens it first). |
| P4-15 | Release v1.1.0 and Phase 4 report (STOP S9) | blocked | e672ac5 (tag v1.1.0) | https://github.com/stas-comp/comphq/actions/runs/35478917941 (full dispatch, all five jobs green); release run https://github.com/stas-comp/comphq/actions/runs/35480026380 (all green incl. publish) | Waiting for owner checks O4.1-O4.4. Image `ghcr.io/stas-comp/comphq:1.1.0` confirmed published (anonymous manifest request returns 200). Reports: `reports/gates-phase-4.md` (54 gates, individually), `reports/phase-4-report.md`, `reports/phase-4/screens/` (38 screenshots from the CI screens job). |
| P5-00 | Spec edits for v1.2 | done | (spec edits, see the P5-00 commit) | | 2026-09-20. SPEC A11 Phase 5, A12, B3, B4, B9.10, B10, B11 and D-70–D-74 written by the owner's spec model, not the build agent. Committed together with `PLAN-v1.2.md` at the start of the P5 build, since they were still uncommitted in the working tree. |
| P5-01 | Storage and the store functions | done | b5bd669 | https://github.com/stas-comp/comphq/actions/runs/35600667365 | `migrations/tasks/0002_checklist.sql` (add-only: one table, one index) and `internal/tasks/checklist.go`: add, tick/untick, rename, remove, restore (Undo), move up/down and to a position, list (live steps, and live+removed for the export). Both limits (50 steps, 200 characters counted as characters) live in the store; every write bumps the change counter in its own transaction and a write that changes nothing bumps nothing. Step add/rename/remove/restore write History rows (`step_*`, with the words in the detail so a later rename can't rewrite the past); ticks and moves never do (D-71) — `details.go`'s `activityText` words them. 22 Go unit tests including the removed-step refusals, restore into a taken position, ordering after a mixed sequence, and a migration test that applies 0002 over a 1.1.0-shaped database and reads an old job back. D-75 records the edge cases SPEC left open. |
| P5-02 | Endpoints, and the no-JavaScript path first | done | 0f2dac8 | https://github.com/stas-comp/comphq/actions/runs/35602179595 | The six POST routes of SPEC B10.2 (`internal/tasks/checklist_handlers.go`), each an ordinary form that redirects back to `/tasks/{id}` (with `#steps`, or `?undo_step=` after a remove); one partial `web/templates/tasks/steps.html`, included by the task page. Built and tested with JavaScript off first: a step's tick is a real `.check` checkbox with an Update button that only exists without script (the design policy allows no custom tick button), rename is a `?rename_step=` view of the page, Undo is a `?undo_step=` view. A refusal (empty, too long, 50 steps, a step somebody else removed) re-renders the page in place with the typed words kept and plain wording that lives in the template; a form carrying `fragment=1` gets the list itself (200, or 422 when refused). 13 Go integration tests and 8 E2E tests (`e2e/tasks/steps-no-script.spec.ts`, JS off, two people on two browsers for gate 5.12, the 50-step limit, axe). |
| P5-03 | The list on screen, in the window and on the page | done | 7484e7e, 9fe8c55 | https://github.com/stas-comp/comphq/actions/runs/35605607872 | `web/static/tasks/steps.js` layered over P5-02's forms: type and press Enter (the box empties and stays focused, and steps typed while one is still on its way queue in order and carry over), tick in place, rename in place (click the words, or Rename; Escape leaves it without closing the window), remove with an Undo that lasts about ten seconds or until the next action, up/down icons and dragging (Sortable, the same vendored library the Board uses), and another computer's changes arriving through the existing change counter (a `data-version` on the list; checked every 60 s and on focus; never swapped over a half-typed step or a rename). Included by the task window (`tasks-window-read`) and the task page, one partial. `GET /tasks/{id}/steps` serves the list alone. `window.js`: typing in Steps no longer counts as an unsaved edit and the keyboard lands on the window's own first field, not a step; a change in the window refreshes the card behind it. 33 E2E tests in `e2e/tasks/steps.spec.ts` (gates 5.01-5.07, 5.09, 5.12, 5.13, 5.16 — each in both the window and the page, axe with 50 steps, 99/99 across three repeats). **Test corrected:** gate 4.23's "no form control in the reading window" now excludes the Steps list (D-76). The first CI run of this push failed on the drag test (the synthetic mouse dropped one row short on the slower runner); fixed in 9fe8c55 with the Board's own technique (wait for the drag to be seen, real pauses between waypoints), 20/20 under full parallel load, then CI green. |
| P5-04 | The badge, History, export and speed | done | fc1fb00 | https://github.com/stas-comp/comphq/actions/runs/35607424651 | A small **3/7** (or a tick when all are done) on cards on the Board, My jobs and Team, none for a job without steps (`tasks-step-badge` partial; `ListBoard` and the shared `listTasks` fetch every job's live-step counts in one query; a step ticked in the window refreshes the card behind it). `tasks.StepsCSV` writes `steps.csv` (Job, Step, Done, Ticked by, Ticked, Removed — removed steps and the steps of removed jobs included, dates quoted where they contain a comma) into Export everything; the README's export line names it. **Ticks never reach History (D-71):** proved at three levels — a store test, an HTTP test that ticks ten steps, and an E2E that ticks ten, unticks five, moves one and asserts History is exactly what it was, then that the four recorded actions do appear. **The Briefing is untouched (D-73):** a store test compares `BriefingTasks` before and after adding steps (DeepEqual) and an `@fresh` E2E compares the page's HTML before and after. The seeder gives one live job in eight a few steps and adds `The fifty steps job` (exactly 50), and `e2e/speed/tasks.speed.spec.ts` gains gate 5.17's tests (a 50-step job opens on its page and in the window within 1.5 s, median of 5; the Board with badges on the 2,000-task library passes the same budget and axe) — all green locally with `npm run test:speed`. |
| P5-05 | The Comp HQ icon | done | cb0ac3f | https://github.com/stas-comp/comphq/actions/runs/35609153225 | The stacked COMP / HQ wordmark of the sidebar (gate 4.13): small wide-spaced white COMP over a large accent-orange HQ on the ink-navy field, drawn once in `tools/icons/comphq.design.svg` (live text in Big Shoulders Display). `npm run icons` (`tools/icons/render.mjs`) embeds the vendored font into `web/static/theme/icons/comphq.svg` and has Playwright's Chromium draw `icon-16/32/48/64/128/192/256/512.png`, `icon-maskable-512.png` (square field, every letter inside the safe circle) and a 6-size PNG-in-ICO `comphq.ico`; 16 and 32 draw HQ alone (COMP is a smudge there), and the two orange-square placeholders are gone. Checked by eye at 16/32/48/64/128/512 on a contact sheet. Tests (`tools/policy/icons_test.go`): every PNG at its declared size; the ICO's directory matches its images; the SVG embeds exactly the vendored font and fetches nothing; navy field and orange HQ at every size; the maskable corners are opaque and no mark pixel lies outside radius 205; COMP absent at 16 and 32 and present from 48; the size lists in the renderer and the test agree. D-77 (one design file; the served SVG is generated from it and not re-drawn in CI because anti-aliasing differs between Windows and Linux). **Test corrected:** gate 4.12's "the icon set is the fifteen line icons" scan now skips `comphq.svg`, the app's own mark and not an interface icon. |
| P5-06 | Linking it, and the manifest | done | 5a006bc | https://github.com/stas-comp/comphq/actions/runs/35610822723 | The `app-head-icons` partial (`web/templates/app/parts.html`) puts the .ico, the 16/32/48/192 PNGs, the apple touch icon, the manifest and `theme-color` #141b2d in every page's head — the frame, the name picker and (through the frame) the 404 page. `manifest.webmanifest`: name/short_name Comp HQ, `id`/`start_url`/`scope` `/` (the Briefing), `display: standalone`, navy `theme_color`, paper `background_color`, all eight sizes as `any` plus the maskable 512. `GET /favicon.ico` answers with the multi-size icon (added to the identity middleware's exemptions so no browser is redirected to the picker for it) and `.webmanifest`/`.ico` are registered MIME types (a bare container has no mime.types file). Tests: Go integration tests (favicon without a cookie, the manifest's fields and every icon it lists at the size it claims, every linked address on the picker and the framed 404), a policy test that the manifest and head name only files that exist at their declared sizes, and E2E (`e2e/design/app-icon.spec.ts`) where Chromium's own `Page.getAppManifest` reads the manifest with no errors, every linked icon decodes at its declared size, and the manifest's start URL lands on the Briefing. **Gate 5.24 has no automatic test (D-78):** the test browser is windowless and reports every bar as visible, so it is owner check O5.3 and a picture; the `--app` window test (gate 1.43) now also asserts it lands on the Briefing. |
| P5-07 | Set up this computer, rewritten | done | 2f60e57 | https://github.com/stas-comp/comphq/actions/runs/35612547378 | Four sections in order, each its own list of steps and each naming its browser: **1** install as an app (Edge: ⋯ → Apps → Install this site as an app; Chrome: ⋮ → Save and share → Install page as app…, or Create shortcut… with Open as window), **2** the `--app=` command for Chrome, Edge and Brave (`BuildSetupCommands` now returns all three; each has its Copy button; the commands wrap rather than scroll, which axe demanded), **3** pin to the taskbar, **4** Download icon (`GET /settings/setup/icon`, an attachment named `Comp HQ.ico`, the same bytes as the linked icon) plus Properties → Change Icon → Browse. Tests: Go unit (three commands, host handling) and integration (section order, browser names, download headers and bytes, needs a person), and `e2e/settings/setup.spec.ts` (order, independence, addresses, Copy for all three, the download, axe and no sideways scroll). **UNVERIFIED WORDING:** the menu wording in sections 1, 3 and 4 was written from knowledge of Edge/Chrome/Windows 11, not from a walk-through of the menus; it has not been checked against the office computers and is not to be presented as checked. The three menu paths to be walked (also asked in the S10 report): (a) Edge ⋯ → Apps → Install this site as an app, (b) Chrome ⋮ → Save and share → Install page as app…, (c) right-click the taskbar icon (or, on Windows 11, the desktop shortcut → Show more options) → Pin to taskbar; and the shortcut Properties → Shortcut tab → Change Icon… → Browse…. Browsers present on the build machine (not walked): Edge 153.0.4234.48, Chrome 153.0.8010.52, Brave 153.1.95.104, Windows 11 Home 10.0.26200; wording last written 2026-09-21. |
| P5-08 | Full sweep: browser check, screenshots, README | in-progress | | | Screens, README, CHANGES and the upgrade/rollback smoke tests written; waiting on the full CI run. |
| P5-09 | Release v1.2.0 and the Phase 5 report (STOP S10) | todo | | | |

## Stop log

| Date | Stop point | Message sent | Owner reply |
|---|---|---|---|
| 2026-09-15 | S1 (P1-02) | GitHub sign-in instructions sent in chat | done (confirmed 2026-09-16) |
| 2026-09-16 | S2 (P1-07) | Make-the-package-public instructions sent in chat | done |
| 2026-09-17 | S3 (P1-39) | Phase 1 report and owner checks prepared (`reports/phase-1-report.md`) | not sent as a stop point — owner explicitly instructed continuing straight into Phase 2 without waiting; see Owner feedback below |
| 2026-09-18 | S4 (P2-20) | Phase 2 report and owner checks sent in chat (`reports/phase-2-report.md`); O1.3 asked about again | owner had no time for the checks and explicitly instructed continuing into Phase 3 with them pending (D-55); asked again at S5 |
| 2026-09-18 | S5 (P3-05) | Phase 3 (final) report and owner checks sent in chat (`reports/phase-3-report.md`); all pending Phase 1 and Phase 2 checks asked about again | waiting |
| 2026-09-20 | S9 (P4-15) | Phase 4 report and owner checks O4.1-O4.4 sent in chat (`reports/phase-4-report.md`); told the owner the task window is the one screen the mockup does not show and to look at it hardest (O4.3); earlier checks O1.x-O3.1 still pending | waiting |

## Owner feedback

| Date | Phase / check | Owner's words | Fix task(s) | Status |
|---|---|---|---|---|
| 2026-09-19 | Phase 3 / O3.2 (look and feel), given while trying v1.0.0 | Design is lacklustre; wants elegant icons (move up/down, remove, give back) instead of worded buttons, and the whole app moved towards the design mockup, Knowledge Base and Calendar included. Wants to assign people from a circle on the card. Reports a fault: a task created with someone assigned shows on the Board but not in My jobs or Team. Wants to click a card and get a Trello-style window with a fuller description, and later a checklist. | Phase 4 (P4-01–P4-15) for everything but the checklist; v1.2 for the checklist | specified, not yet built |
| 2026-09-18 | Phase 3 / O3.1–O3.2 | (none yet — report sent at S5) | — | pending |
| 2026-09-18 | Phase 2 / O2.1–O2.2 (+ O1.3) | (none yet — owner instructed the build agent to mark these pending and start Phase 3 rather than wait) | — | pending |
| 2026-09-17 | Phase 1 / O1.1–O1.4 | (none yet — owner instructed the build agent to mark these pending and start Phase 2 early rather than wait) | — | pending |
