# Prompt for the v1.3 build agent (Claude Sonnet, auto)

Paste everything below the line into a fresh Claude Code session opened in
`C:\Users\Knacker\Desktop\Staff HQ`, on Sonnet, in auto-accept mode.

---

You are the build agent for Comp HQ, continuing a project that has already shipped v1.2.0. Build
**Phase 6 (v1.3.0)** from start to finish, on auto. Don't stop to ask me anything except at the stop
points named below.

**Read these first, in this order, before touching anything:**

1. `PLAN.md` §2: how you work. That covers sessions, the progress log, the git workflow, the
   definition of done, what to do when CI fails, the command surface, and the standing engineering
   rules. They apply to every task in this phase exactly as they did in Phases 1–5.
2. `PROGRESS.md`: where the project actually is.
3. `PLAN-v1.3.md`, the whole of it. This is your task list for this phase. §5 holds tasks
   **P6-01 to P6-08** in order, each with its gates, what it touches, how it must behave and which
   tests prove it.
4. `SPEC.md`: **A11 "Phase 6"** (gates 6.01–6.41 and owner checks O6.1–O6.4) and **B12**. Also B4's
   new "Dates typed without a year" entry and its half-typed-word line under "Article blocks and
   search". SPEC is the contract, and `PLAN-v1.3.md` is the order of work. Where they disagree, SPEC
   wins and you write a decision entry saying so.
5. `docs/decisions.md`: D-80 to D-85 are the decisions already settled for this phase. Don't
   re-open them, and don't ask me about them. D-61 (the typed date box) and D-79 (the search flake)
   are the background to D-81, D-82 and D-85.

**What you're building, in one line:** the sidebar and top bar stay put, every date box gets a
day-first calendar and accepts a date without its year, the Calendar starts on Sunday and a blank
day adds an event, and search finds half-typed words.

**Start by** adding rows `P6-01` … `P6-08` to the `PROGRESS.md` table with status `todo`, in order.
`P6-00` (the spec work) is already done. Mark it `done`, dated 2026-09-22, with no commit of its own
beyond the spec edits already on `main`.

**Then work P6-01 through P6-08 in order**, one task per unit of work, following `PLAN.md` §2.4's
definition of done for each: tests written and green locally via `npm run check`, pushed, CI green
on that push before the next task starts, `PROGRESS.md` updated with the commit and the CI run URL,
decision entries written where the task names them, and the README updated in the same task if the
task changes anything I will see or do.

## Things that are easy to get wrong here

- **The typed path first** (P6-02 before P6-03). The server must understand **25/9** before any
  script exists, and be tested that way with JavaScript off. The calendar writes into the text box.
  It is never the only way to enter a date (gate 6.17).
- **One rule, two places.** The nearest-year rule of D-82 runs on the server (`internal/app/format`)
  and in the calendar script, where the box is rewritten on leaving. Test both against the same
  table of dates, so the two cannot drift apart.
- **"Today" is `app.Today`, never the wall clock**, on the server and in tests. Pin it in every date
  test. Test "This Saturday" on a Saturday and on a Wednesday.
- **Gate 4.11 stays at four icon-only buttons.** The calendar opens from the date box itself. The
  calendar mark inside the box is a background image, not a button (D-81).
- **Sticky has one real risk: overlays** (gate 6.03). After scrolling, check the task window, the
  live search panel, a card's name menu, a drag in progress, the steps Undo and the date calendar
  all sit above the top bar. Check that gate 1.28's highlighted passage lands *below* the bar.
  Check each one; don't assume any.
- **Sunday-first corrects earlier tests. It doesn't delete them.** The Monday-first assertions (at
  least `e2e/calendar/calendar.spec.ts` and `e2e/design/calendar.spec.ts`; search for more) are
  changed to Sunday-first, name gate 6.20, and are listed under "Tests corrected" in the phase
  report with D-83 as the reason. Nothing else in gate 2.12 changes. **The Briefing must not change**:
  write the test that pins it.
- **Search: the rebuild at start-up is the point** (D-85). A second word table that is only written
  on publish would go stale after a rollback to 1.2.0 and an upgrade again. Prove gate 6.34 in the
  container test's rollback step, not by assertion. The migration is add-only, a new file, never an
  edit to an existing migration (the policy test enforces this). Raw input still never reaches MATCH.
- **Leave D-79's test alone.** Gate 1.29's test and its note stay as they are. 6.30's tests are new.
- **Pushes and CI** (these each cost a wasted cycle in Phase 5):
  - A push containing any `[skip ci]` commit skips CI for the *whole* push. Never push a progress
    note marked `[skip ci]` together with a task's code. Fold `PROGRESS.md` updates into the next
    task's commit.
  - CI takes 15–25 minutes a push. Push once per task with `npm run check` green first, and watch
    the run in the background.
- **Line endings.** This machine's Python writes CRLF in text mode. Read and write with
  `newline=''`, never run a line-ending fixer over images, and write regex-heavy TypeScript with the
  file-writing tool rather than a shell heredoc (heredocs can lose backslashes).
- **Screen pictures.** This session cannot photograph the screen or open a visible browser. Use
  Playwright-rendered screenshots for the README and the report, and leave the look to the owner
  checks.

## When to stop

- **S11**, at P6-08, once v1.3.0 is released and `reports/phase-6-report.md` is written. Use the
  shared phase-report message in `PLAN.md` §5.1, and include owner checks O6.1–O6.4 with what to try
  and what I should see. Say plainly that the date calendar has no mockup, so O6.2 matters most.
- **S6**, only if a Part A gate turns out to be impossible or self-contradictory. Give me options
  with a recommendation, and keep working on anything that doesn't depend on the answer.
- **S7**, only after three genuine attempts at the same failure: mark the task `blocked`, write
  `reports/blocker-P6-NN.md` in plain language, and send the short version.

Never ask me a technical question. Never weaken, skip or delete a test to get to green. If a test is
genuinely wrong because it contradicts SPEC Part A, fix the test, write the decision entry, and list
it under "Tests corrected" in the phase report.

Release as `v1.3.0`, upgrading from and rolling back to `v1.2.0`, following the release procedure in
`PLAN.md` §5.2. Commit messages start with the task ID and end with the co-author line your session
gives you.

Begin with P6-01.
