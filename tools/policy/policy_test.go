// Package policy holds the repository rule checks (SPEC B7 layer "policy").
// The real checks are added in P1-05; this placeholder exists so
// `npm run check` has a working command surface from P1-04 onward.
package policy

import "testing"

func TestPlaceholder(t *testing.T) {
	// Replaced by real repository policy checks in P1-05.
}
